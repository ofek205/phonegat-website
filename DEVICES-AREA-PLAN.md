# אזור מכירת המכשירים — תוכנית ביצוע לאישור

מבוסס על `תוכנית מלאה להרחבת אזור מכירת המכשירים באתר פון גת.odt` (4,868 מילים) ועל סקירת האתר
והפרויקט. הסעיפים כאן ממוספרים לפי 16 הפריטים שהתוכנית דורשת תחת "תוצר נדרש לפני תחילת היישום",
כדי שאפשר יהיה לעבור עליהם אחד אחד.

**שני דברים שצריך להחליט לפני שנוגעים בקוד** מסומנים ⛔ ומרוכזים בסוף.

---

## 1. ה-Skills שנבדקו

בפרויקט קיימים **ארבעה** Skills. התוכנית מבקשת עשרים.

| Skill | קיים | רלוונטי לאזור המכשירים |
|---|---|---|
| `pg-new-content-page` | ✅ | **קריטי.** התקן המחייב לכל דף חדש |
| `muzli-editorial-design` | ✅ | ✅ טיפוגרפיה, מרווחים, שפת העיצוב |
| `israeli-accessibility-compliance` | ✅ | ✅ ת"י 5568, תפריט ההתאמות, טבלאות נגישות |
| `hebrew-chatbot-builder` | ✅ | ✖ לא נוגע לאזור הזה |

### 17 ה-Skills שאינם קיימים, ומה ממלא את מקומם

זה לא פער שצריך לסגור בבניית Skills חדשים. התפקוד קיים בפרויקט בצורות אחרות:

| מה התוכנית ביקשה | מה קיים בפועל |
|---|---|
| SEO, מחקר מילות מפתח | `.claude/knowledge/seo-plan.md` · זיכרון `phonegat-keyword-research` · Search Console מאומת |
| קופירייטינג, כתיבה אנושית | `.claude/knowledge/04-copywriter.md` · **`pg-new-content-page` §7** — רשימת סימני הכתיבה המכונית, הכלי המדויק ביותר שיש כאן לנושא |
| UX, ארכיטקטורת מידע | `.claude/knowledge/03-ux-ui.md` |
| עיצוב UI, מובייל | `muzli-editorial-design` + §2–§4 ב-`pg-new-content-page` |
| נגישות | `israeli-accessibility-compliance` |
| QA, בדיקת קוד, בדיקת קישורים | **`.claude/preflight.js`** — 20+ בדיקות שחוסמות דחיפה · `.claude/knowledge/09-qa.md` |
| Structured Data, בדיקת Schema | preflight בדיקות 9 · Rich Results Test ידנית |
| ביצועים | מדידה בדפדפן לפי §10 ב-`pg-new-content-page` |
| מחקר אונליין, בדיקת עובדות | `WebSearch` / `WebFetch` |
| אנליטיקס, אופטימיזציית המרות | GTM (יצירת תגים ידנית) · §6 ב-`pg-new-content-page` |

קיימים גם 11 סוכני `pg-*` (PM, UX, קופירייטר, QA, אבטחה וכו'). **לא אפעיל אותם בלי שתבקש** —
ההנחיות שלי אוסרות זאת. הידע שהם נושאים יושב באותם קבצי `.claude/knowledge/` ואני קורא אותו ישירות.

---

## 2. איזה Skill בכל שלב

| שלב | Skills / כלים בפועל |
|---|---|
| תשתית ורכיבים | `pg-new-content-page` §0–§4 · `muzli-editorial-design` |
| כל דף חדש | `pg-new-content-page` (סקריפט הפיגום, 14 פריטי המסגרת) |
| כתיבת תוכן | `pg-new-content-page` §7 · `knowledge/04-copywriter.md` |
| מפרטים ונתונים | `WebSearch` / `WebFetch` מול אתרי יצרנים |
| SEO | `knowledge/seo-plan.md` · Search Console |
| Schema | preflight 9 · Rich Results Test |
| נגישות | `israeli-accessibility-compliance` · מדידה ב-375px |
| QA לפני דחיפה | `preflight.js` · §10 (מדידה, לא הסתכלות) |

---

## 3. מפת הדפים

### מה שקיים היום ולא נוגעים בו

```
/                                        דף הבית
/phone-problems/                         מדריך תקלות
/mobile-phone-repair-kiryat-gat/         מעבדת סלולר (מרכז)
/iphone-repair-kiryat-gat/               תיקון אייפון
/phone-screen-replacement-kiryat-gat/    החלפת מסך
/phone-battery-replacement-kiryat-gat/   החלפת סוללה
/charging-port-repair-kiryat-gat/        תיקון שקע טעינה
/contact/                                יצירת קשר
/privacy.html  /accessibility.html       משפטי
```

### האזור החדש

```
/phones/                          מרכז המכשירים            CollectionPage
/phones/all/                      כל המכשירים + סינון      CollectionPage + ItemList
/phones/iphone/                   מותג Apple               CollectionPage + ItemList
/phones/samsung/                  מותג Samsung             CollectionPage + ItemList
/phones/xiaomi/                   מותג Xiaomi              CollectionPage + ItemList
/phones/<device-slug>/            עמוד מכשיר               Product + Brand
/phones/compare/                  כלי השוואה               ⚠ noindex — ראה §9
/phones/find-my-phone/            שאלון                    WebPage
/compare/                         מרכז השוואות             CollectionPage
/compare/<a>-vs-<b>/              עמוד השוואה              Article
/guides/                          מרכז מדריכים ⚠ חסר בתוכנית  CollectionPage
/guides/official-vs-parallel-import/   יבוא רשמי מול מקביל  Article
/guides/<guide-slug>/             7 מדריכי קנייה נוספים    Article
/upcoming-phones/                 מרכז מכשירים עתידיים     CollectionPage
/upcoming-phones/<slug>/          עמוד שמועות              Article
```

### שלוש הערות על המבנה

**מרכז המדריכים חסר בתוכנית.** התוכנית מגדירה שמונה עמודים תחת `/guides/` אבל לא את `/guides/`
עצמו. בלעדיו אין breadcrumb תקין (`בית › מדריכים › בחירת טלפון`) ואין דף שאוסף את המדריכים.
**הצעה: להוסיף אותו.**

**המבנה החדש עמוק יותר מהקיים.** עמודי השירות יושבים בשורש (`/iphone-repair-kiryat-gat/`).
המבנה החדש מקנן (`/phones/iphone/`). זה שיפור, לא סתירה, ואין לגעת בכתובות הקיימות (כלל 19).
אבל **זה בדיוק החסם בסעיף 15.1** — הכלים בפרויקט לא יודעים לטפל בשתי רמות.

**`/phones/iphone/` מול `/iphone-repair-kiryat-gat/`** — כוונת חיפוש שונה (קנייה מול תיקון), ולכן
אין קניבליזציה. **חייבים לקשר הדדית**: מי שהגיע לתיקון וגילה שלא שווה לתקן, ומי שקונה חדש ורוצה
לדעת מי יתקן.

---

## 4. מפת הניווט

הניווט היום **תשעה פריטים, זהים בכל הדפים** (preflight אוכף). כל שינוי בו נוגע ב-10 הדפים הקיימים
ובכל דף חדש.

```
מי אנחנו · ביקורות · שירותים · מדריך תקלות · מבצעים · כתבו עלינו · מיקום · שאלות נפוצות · צרו קשר
```

**ההצעה: פריט אחד חדש.**

```
מי אנחנו · ביקורות · שירותים · [מכשירים] · מדריך תקלות · מבצעים · כתבו עלינו · מיקום · שאלות נפוצות · צרו קשר
```

`מכשירים` → `/phones/`, ממוקם אחרי `שירותים` כי זו הרחבה שלו.

**למה פריט אחד ולא שלושה.** עשרה פריטים בעברית במובייל צריכים מדידה ממשית ב-375px לפני שמחליטים.
כל פריט נוסף (`מדריכים`, `השוואות`) מגדיל את הסיכון לגלישה. המרכז `/phones/` יקשר פנימה לכל השאר,
וזו גם ארכיטקטורת המידע הנכונה.

**חוב קיים שמצאתי:** `/mobile-phone-repair-kiryat-gat/` קיים אבל **אינו בניווט כלל**. הפוטר מכיל רק
טלפונים, רשתות חברתיות ושני הדפים המשפטיים, בלי אף קישור לעמודי תוכן. לא אתקן את זה כאן בלי שתאמר,
אבל שווה לדעת שהאזור החדש נכנס לאתר שבו הפוטר לא מקשר לשום עמוד.

**סרגל המובייל (`nav.mbar`) לא משתנה.** שלושת הפריטים בו (חייגו / WhatsApp / ניווט) הם פעולות מהירות,
לא ניווט. הוספת "מכשירים" לשם תחליף פעולה שמייצרת פניות בקישור לדף.

---

## 5. הכפתורים והקישורים הקיימים שצריך לשנות

תשע העמודות שהתוכנית ביקשה, מקוצרות. **אין לשנות אף אחד מהם לפני שהדף שהוא מפנה אליו קיים ועובד.**

### א. אריח "מכירת מכשירים" — `index.html` › `#services`

| | |
|---|---|
| מה כתוב | `מכירת מכשירים` · תקציר: "מכשירים חדשים מכל המותגים, בתשלומים בתנאים נוחים ובליווי אישי" |
| לאן מוביל היום | `https://wa.me/97286812050?text=היי, אשמח לפרטים על השירות של פון גת` |
| הבעיה | זה **בדיוק אותו נוסח** שיש בחמשת האריחים האחרים. אין דרך לדעת מי לחץ על מכירת מכשירים |
| לאן צריך | כפתור ראשי → `/phones/` · כפתור WhatsApp נשאר, עם prefill ייחודי |
| טקסט | הראשי: `לצפייה במכשירים`. ה-WhatsApp נשאר `WhatsApp` |
| עיצוב | הראשי `btn btn-teal btn-sm`, ה-WhatsApp נשאר ירוק. **כפתור שכתוב עליו WhatsApp חייב להיות ירוק** |
| מקום | ללא שינוי |
| מובייל | כן, האריחים כבר רספונסיביים |
| מעקב | ה-prefill הייחודי נותן פילוח ב-GTM בלי קוד. הראשי צריך `phone_view` |

**עדיפות: הגבוהה ביותר.** זו נקודת החיבור המרכזית מדף הבית.

### ב. שקופית "מכשירים חדשים, ועוזרים לכם לבחור" — `index.html` › `#deals`

| | |
|---|---|
| מוביל היום | `wa.me/…?text=היי, אשמח לפרטים על מכשיר חדש בתשלומים` (`שלחו הודעה ב-WhatsApp`) |
| לאן צריך | **ה-WhatsApp נשאר כמו שהוא.** להוסיף כפתור שני → `/phones/` |
| למה לא להחליף | ה-prefill הזה כבר מפולח וכבר ממיר. שקופית מבצע היא הקשר "רוצה לדבר", לא "רוצה לקרוא" |
| טקסט הנוסף | `לצפייה במכשירים` |

### ג. שקופית "העברת נתונים" — `index.html` › `#deals`

| | |
|---|---|
| מוביל היום | `wa.me/…?text=היי, אשמח לתאם העברת נתונים למכשיר חדש` |
| לאן צריך | ללא שינוי. אפשר לקשר מכאן ל-`/phones/` כשירות נלווה, בעדיפות נמוכה |

### ד. גשר מהמעבדה למכירה — 5 עמודי השירות + `/phone-problems/`

| | |
|---|---|
| הבעיה | מי שקורא "המסך שלי שבור" הוא לפעמים מי שיחליט לקנות חדש. אין לו לאן ללכת |
| לאן צריך | קישור בהקשר של "כשלא שווה לתקן" → `/phones/` |
| למה זה חשוב | זו הכוונה המסחרית הגבוהה ביותר באתר, וכרגע היא לא מנוצלת |
| אזהרה | **בהקשר בלבד, לא באנר.** עמוד תיקון שדוחף למכירה מאבד אמון, וזה בדיוק מה שהתוכנית אוסרת |

### ה. תשתית שמתעדכנת עם כל דף

| | |
|---|---|
| `prototype/sitemap.xml` | דף מצטרף **בהשקה לפרודקשן בלבד**, לא בפיגום (מוסכמת `new-page.js`) |
| `prototype/sw.js` › `SHELL` | כתובת חדשה + **החלפת שם המטמון** (`pg-v4` → `pg-v5`), אחרת מבקר חוזר נשאר עם הרשימה הישנה לנצח |
| `prototype/services.json` | פאנל הסקירה בטסטים קורא ממנו. הדפים החדשים צריכים להירשם כדי להופיע |
| ניווט ב-10 הדפים הקיימים | הפריט `מכשירים` (סעיף 4) |

### מה לא נוגעים בו

חמשת האריחים האחרים ב-`#services` · `nav.mbar` · הפוטר (אלא אם תחליט אחרת) · כל כתובת קיימת ·
טקסט של ביקורת לקוח.

---

## 6. הרכיבים החדשים

הסקירה מצאה ש**אין באתר אף טבלה ואף Tabs**. כלומר טבלת המפרט וטבלת ההשוואה הן רכיבים חדשים לגמרי,
ולא התאמה של קיים.

### ⚠ תיקון: הגרסה הראשונה של הרשימה הזאת סתרה את מערכת העיצוב

`muzli-editorial-design` אוסר במפורש: `border-radius` על כל דבר שאינו כפתור · רקע מוצל או מוגדר
מאחורי טקסט · `box-shadow` על רכיב תוכן · **"ארבעה כרטיסים שווים בגריד"** · ו-`<span>` בצורת גלולה
בתור תג לקטגוריה, לשם מכשיר או לסינון.

כלומר "כרטיס מכשיר", "תג מלאי" ו"תג רמת אמינות" — שלושתם, כפי שנכתבו — היו אנטי-פטרנים.
התיקון אינו קוסמטי: למערכת העיצוב **יש מתכון מוכן בדיוק לתוכן הזה**, והוא גם פותר את בעיית המובייל
בדרך אחרת ממה שהנחתי.

> **Labelled variants (per-device instructions, specs, comparisons)** — a two-column grid: `8rem`
> label column in sans 700, text column beside it, `border-top` hairline per row. Collapses to
> stacked label + text under 640px. **Not cards.**

| רכיב | תפקיד | הצורה, לפי מערכת העיצוב |
|---|---|---|
| **שורת מכשיר** (לא כרטיס) | 14 שדות לפי התוכנית | שורה ברשימה, מופרדת בקו שערה. **לא כרטיס, לא גריד של ארבעה, לא צל, לא רדיוס** |
| טבלת מפרט | 35 שורות + מקור ותאריך | גריד דו-טורי: תווית `8rem` במשקל 700, ערך לצידה, `border-top` לכל שורה. מתחת ל-640px נערם |
| טבלת השוואה | 2–3 עמודות + "הציגו רק את ההבדלים" | אותו דפוס. **הכי מסובך במובייל** — ראה השאלה הפתוחה למטה |
| סרגל סינון | 7 סינונים ראשוניים | `.picker`/`.chip` הקיים. **החלטה מפורשת** — ראה למטה |
| מגש השוואה | דביק, אוסף 2–3 מכשירים בין דפים | `localStorage`. פינוי 70px ל-`nav.mbar`. ללא רדיוס וללא צל |
| שאלון | 12 שאלות, נפתח רק בלחיצה | **לא מוצג אוטומטית** (כלל 14). זה **טופס** — ראה §16 |
| אזור יבוא ואחריות | רשמי מול מקביל, 10 שדות כל אחד | גריד דו-טורי + מתכון ההערה. **לא "תיבה"**: אין רקע ואין מסגרת מארבעה צדדים |
| **מצב מלאי** (לא תג) | 6 מצבים | מתכון ההערה: `border-inline-start:3px solid` + מילות הפתיחה נושאות את הצבע. אין גלולה |
| המלצת סיגל / הטיפ של ברוך | ניתן לעריכה + סטטוס אישור | **לא מופיע בלי `status: approved`** |
| הערת מקור ותאריך | תחתית כל מפרט | הנוסח מהתוכנית, מילה במילה |
| **רמת אמינות** (לא תג) | 4 רמות בעמודי שמועות | אותו מתכון הערה. רשמי / דיווח מבוסס / שמועה מוקדמת / לא ידוע |
| טבלת מעקב דיווחים | 9 עמודות | אותו דפוס טבלה |
| Empty state | "אין מכשיר שעונה לסינון" | טקסט וקו שערה. **לא תיבה מוצלת** + הצעה לפנות לחנות |

### הכרעה: צ׳יפים של סינון מותרים, תגים לא

יש כאן מתיחות אמיתית. האנטי-פטרן אוסר גלולה "בתור תג לסינון", אבל `phone-problems/index.html` —
דף תוכן שהמערכת הזאת כן חלה עליו — כבר עלה לאוויר עם `.picker`/`.chip` לבחירת מכשיר.

**ההכרעה: צ׳יפ שהוא בקרה אינטראקטיבית הוא כפתור, וכפתורים הם המקום היחיד שבו המערכת נכנעת לאתר**
(רדיוס 4px, משקל 700). צ׳יפ **דקורטיבי** שרק מציג מידע אסור. לכן סרגל הסינון שומר על הצורה הקיימת
במדריך, ומצב המלאי ורמת האמינות אינם צ׳יפים.

זה נרשם כאן במפורש כדי שלא "יתוקן" בכיוון ההפוך בעוד חודש.

### ⚠ הבידוד של הטקסט הלטיני — הבאג הוודאי ביותר באזור הזה

`muzli-editorial-design` דורש: *"Latin strings (`WhatsApp`, `iPhone`, phone numbers) go in
`<bdo dir="ltr">` or `.ltr`"*, ו-`israeli-accessibility-compliance` דורש `dir="ltr"` על תא בטבלה
שתוכנו לטיני.

**זו לא הערה קוסמטית באזור הזה.** טבלת מפרט בעברית מכילה מאה מחרוזות לטיניות בשורה:
`iPhone 17 Pro Max` · `Snapdragon 8 Elite` · `6.9″` · `A19 Pro` · `IP68` · `120Hz` · `5,000mAh` ·
`2778×1284` · `USB-C 3.2`. אלגוריתם ה-bidi של הדפדפן מסדר אותן מחדש בהקשר RTL, והתוצאה היא
`Pro Max iPhone 17` או מידה שהופכת סדר. **כל שדה מפרט וכל תא נתונים חייב בידוד LTR**, ואין
דרך לתפוס את זה חוץ מהסתכלות על הדף.

זה גם נכון לקורא מסך: Step 8 בסקיל הנגישות מדגים `<td dir="ltr">` בדיוק לתאים כאלה.

### שאלה פתוחה: טבלת השוואה של 3 מכשירים ב-375px

מערכת העיצוב אומרת שהדפוס הדו-טורי **נערם** מתחת ל-640px. אבל השוואה שנערמת מאבדת את מה שהיא
קיימת בשבילו: אי אפשר להשוות שני מכשירים שלא נמצאים באותה שורה.

שלוש אפשרויות, וזו החלטת עיצוב שצריך לקבל לפני שבונים: גלילה אופקית עם טור תוויות דביק ·
שני מכשירים בלבד במובייל ושלושה בדסקטופ · או "מכשיר מול מכשיר" בהחלפה. **אני ממליץ על הראשונה**,
כי היא היחידה ששומרת על ההשוואה עצמה, אבל היא דורשת מדידה ב-375px לפני שמתחייבים.

---

## 7. הרכיבים הקיימים שישמשו מחדש

| קיים | מאיפה | לאיזה שימוש |
|---|---|---|
| כל המסגרת המשותפת (14 פריטים) | `pg-new-content-page` | כל דף חדש. **בפיגום, לא בהעתקה ידנית** |
| `.tile` כרטיס תוכן | `index.html` | מסלולי המרכז, כרטיסי מדריכים, מותגים |
| אזור CTA | עמודי שירות | תחתית כל דף |
| Breadcrumbs | עמודי שירות + מדריך | כל הדפים החדשים |
| `<details>` accordion | מדריך + שירות | קיבוץ מפרט, שאלות נפוצות |
| `.picker` / `.chip` | `index.html` + מדריך | הבסיס לסרגל הסינון |
| כפתורי WhatsApp | כל הדפים | כל CTA, עם prefill ייחודי |
| אזור ביקורות | `index.html` + מרכז המעבדה | חיזוק אמון במרכז המכשירים |
| שלבים ממוספרים | מדריך + שירות | "מה לבדוק לפני קנייה" |
| תיבת הערה | מדריך + שירות | אזהרות יבוא ואחריות |
| `.fig .ph` פלייסהולדר | מדריך | **בדיוק מה שכלל 5 מבקש** — מקום לתמונה עם `alt` שנכתב מראש |
| FAQ schema | 8 דפים | מדריכים והשוואות |

---

## 8. מודל הנתונים

### הבעיה שצריך לפתור

התוכנית דורשת "מקור נתונים אחד" (54 שדות למכשיר) ו"מערכת ניהול". **בפרויקט אין CMS, אין
`package.json`, ואין שלב build** — וזו החלטה מכוונת. במקביל, `pg-new-content-page` §9 דורש שכל
התוכן יהיה ב-HTML **עם JavaScript מכובה**, אחרת הדף מאבד את תוכנו בגוגל.

### הפתרון: סקריפט מחולל, לא שלב build

```
prototype/devices.json          ← מקור אמת יחיד (54 שדות, כולל מקורות וסטטוס אישור)
        ↓  node .claude/tools/gen-devices.js     ← מורץ מקומית, לא בפריסה
prototype/phones/<slug>/index.html    ← HTML סטטי, מקומט לגיט
prototype/devices-public.json         ← תת-קבוצה לזמן ריצה (סינון, השוואה, שאלון)
```

זה שומר על שלושת האילוצים יחד: מקור אמת אחד · אין שלב build בפריסה · התוכן ב-HTML בלי JS.
תקדים בפרויקט: `services.json` + `new-page.js` עובדים כך בדיוק.

### שני קבצים ולא אחד

`vercel.json` **מפנה היום את `/services.json` לדף הבית בפרודקשן** כדי שהמניפסט לא יהיה קריא לציבור.
`devices.json` יכיל מקורות, הערות פנימיות וסטטוס אישור של המלצות — דברים שאין להם מה להיות חשופים.
אבל הכלים האינטראקטיביים **חייבים** לקרוא נתונים בזמן ריצה.

לכן: `devices.json` פרטי (מוגן באותו כלל הפניה), ו-`devices-public.json` נגזר ממנו ומכיל **רק מה
שכבר מוצג בעמוד**. אין דרך לדלוף משהו שלא הוצג.

### השדות

54 השדות מהתוכנית, בארבע קבוצות:

| קבוצה | דוגמאות | מקור |
|---|---|---|
| זהות (1–7) | יצרן, דגם, slug, סדרה, סטטוס, שנת השקה, מערכת הפעלה | אתר היצרן |
| מפרט (8–23, 33) | מסך, מעבד, RAM, אחסון, מצלמות, סוללה, מידות, משקל, SIM | **אתר היצרן בלבד** + `spec_source` + `spec_checked` |
| מסחרי (24–32) | מחיר, מלאי, יבוא, יבואן, אחריות, תשלומים | **ממך בלבד. אין לחקור ואין להסיק** |
| עריכה ו-SEO (35–54) | יתרונות, חסרונות, המלצות, מילות מפתח, Title, Schema | נכתב, עובר אישור |

**שני שדות שמונעים את התקלה שהתוכנית מפחדת ממנה:**

- `spec_source` + `spec_checked` **לכל שדה מפרט**, לא לכל מכשיר. אחרת אין דרך לדעת איזה נתון נבדק מתי.
- `recommendation_status: draft | approved` — טיוטה **לא נכנסת ל-HTML המחולל**. בדיקת preflight חדשה תאכוף.

---

## 9. תוכנית SEO

### לכל עמוד, לפני כתיבה

מילת מפתח ראשית · משניות · Search Intent · שאלות · מתחרים · Title · Meta · H1 · H2 מרכזיים ·
קישורים פנימיים · CTA · Schema · תאריך בדיקה ב-Search Console. (14 הפריטים מהתוכנית.)

### ארבע החלטות שמונעות תקלה

**`/phones/compare/` יקבל `noindex, follow` + canonical ל-`/compare/`.** זה כלי עם פרמטרים, בלי
תוכן ייחודי, ושתי כתובות שמכוונות ל"השוואת טלפונים" מקניבלות זו את זו. `/compare/` הוא העמוד
שמתחרה בגוגל, `/phones/compare/` הוא הכלי. פותר גם את הקניבליזציה וגם את התוכן הכפול מהפרמטרים.

**הסינון ב-`/phones/all/` לא יוצר כתובות.** הסתרה בצד לקוח בלבד, כל הכרטיסים ב-HTML.
§9 ב-`pg-new-content-page` מתעד בדיוק את המלכודת הזאת מהמדריך: סינון שמסתיר כברירת מחדל ומגלה
ב-JS מוחק את רוב התוכן מבחינת הסורק. הבדיקה:

```bash
curl -sS https://www.phonegat.co.il/phones/all/ | node -e "…ספירת מילים ומספר כרטיסים…"
```

**`/phones/find-my-phone/` צריך תוכן סטטי אמיתי.** התוצאות הן JS, ולכן בלי הסבר סטטי על איך בוחרים
מכשיר זה עמוד רזה. הסבר בעברית שעומד בפני עצמו הוא גם מה שמצדיק את קיומו לגולש.

**חפיפה בין מדריך להשוואה.** `/guides/iphone-or-galaxy/` (החלטה כללית) הוא ההורה;
`/compare/iphone-17-vs-galaxy-s26/` (דגמים ספציפיים) הוא הבן. המדריך מקשר למטה, ההשוואה מקשרת למעלה.
אחרת שני העמודים מתחרים ושניהם מפסידים.

**⚠ החפיפה החמישית, וזו שלא ראיתי בגרסה הראשונה: `/phones/` מול דף הבית.**
`.claude/knowledge/seo-plan.md` מגדיר את **`קניית מכשיר/טלפון קרית גת` כמילת מפתח ראשית**, ודף הבית
תובע אותה במפורש: ה-`<title>` שלו הוא *"פון גת | תיקון וקניית סלולר בקרית גת"*. כלומר `/phones/`
ייכנס לתחרות מול הדף החזק ביותר באתר, על הביטוי המסחרי הכי חשוב שיש לו.

**ההמלצה: לא לתקן את דף הבית** (כלל 19 — לא לפגוע ב-SEO קיים), אלא לתת ל-`/phones/` לתבוע את
הביטויים המסחריים הספציפיים (`קניית אייפון קרית גת`, `מחירי טלפונים קרית גת`, `טלפון בתשלומים`)
ולהשאיר לדף הבית את המיתוג והתיקונים. הקישור הפנימי מדף הבית ל-`/phones/` הוא הסיגנל שאומר לגוגל
מי העמוד לקנייה. **זה משהו לעקוב אחריו ב-Search Console אחרי ההשקה, לא לפתור מראש** — עמוד שבאמת
מציג מכשירים יגבר בסוף על עמוד שרק מזכיר אותם.

**ביטוי אחד שקיים בתוכנית ה-SEO ואינו באזור הזה בכלל: `מכשירים יד 2`.** הוא רשום כמילת מפתח משנית
ואין לו עמוד. התוכנית עוסקת רק במכשירים חדשים. **שאלה אליך: האזור צריך לכסות גם יד שנייה?**
זה משנה את מודל הנתונים (מצב מכשיר, אחריות שונה לגמרי), ולכן זו החלטה עכשיו ולא אחר כך.

### קישורים פנימיים

לפי התוכנית, ובתוספת שני קישורים שהיא לא ביקשה ושהם החשובים ביותר מסחרית:

- **מעבדה → מכירה**: מ-5 עמודי השירות ומהמדריך, בהקשר "כשלא שווה לתקן"
- **מכירה → מעבדה**: מכל עמוד מכשיר, בהקשר "מי יתקן אם משהו יקרה"

זה מה שהופך את שני צדי העסק לאתר אחד ולא לשני אתרים שחולקים פוטר.

---

## 10. תוכנית מחקר מילות מפתח

**אין גישה ל-Keyword Planner.** מה שכן זמין: Search Console (מאומת) · השלמות אוטומטיות · People Also
Ask · Related Searches · אתרי יצרנים · מתחרים ישראליים · `.claude/knowledge/seo-plan.md` ·
זיכרון `phonegat-keyword-research` (מחקר עומק מיולי 2026).

**אין להמציא נפחי חיפוש.** כל ביטוי בלי נתון אמיתי מוצג כ**מועמד למחקר**, לא כעובדה. זה כלל התוכנית
ואני אאכוף אותו בכתיבה.

מהמחקר הקיים, שני ממצאים שנוגעים ישירות לאזור הזה: **פער בקטגוריות Xiaomi ו-Galaxy A**, ו**"מחירון"
כביטוי עם ביקוש שאין לו כרגע עמוד**. שניהם משרתים את `/phones/all/` ואת עמודי המותגים.

---

## 11. תוכנית Structured Data

### לפי סוג עמוד

| עמוד | Schema | מה **אסור** |
|---|---|---|
| מכשיר | `Product` + `Brand` + `BreadcrumbList` + `#business` | **אין `Offer`, אין `availability`, אין `aggregateRating`** עד שיהיו מחיר ומלאי אמיתיים ומוצגים |
| מותג / מרכז / כל המכשירים | `CollectionPage` + `ItemList` + `BreadcrumbList` | אין דירוג |
| מדריך | `Article` + `BreadcrumbList` + author + publisher + datePublished/dateModified | אין `Product` |
| השוואה | `Article` + `BreadcrumbList` | **אין דירוג מומצא, אין הכרזת מנצח** |
| שמועות | `Article` + `BreadcrumbList` + author + publisher + תאריכים + `Image` | **אין `Product`, `Offer`, מחיר, מלאי, דירוג, ביקורות** |
| כלי השוואה | `WebApplication` או ללא | noindex |

`#business` (`MobilePhoneStore`) **מוגדר** בכל דף, לא רק מוזכר ב-`@id` — preflight דורש.

### `Product` בלי `Offer` — מה זה אומר בפועל

חוקי לגמרי, אבל **לא זכאי לתוצאות עשירות של מוצר בגוגל**. זה המצב עד שיהיו מחירים אמיתיים, וזה
עדיף על החלופה: `Offer` עם מחיר שלא מתעדכן הוא גם הצהרה שגויה לגוגל וגם חשיפה צרכנית.

### אימות

preflight 9 (JSON תקין בכל דף) → Rich Results Test → Schema Validator → בדיקת Live URL →
**התאמה בין ה-Schema למה שמוצג בעמוד בפועל**. האחרון הוא היחיד שאף כלי לא בודק, והוא זה שהתוכנית
מדגישה.

---

## 12. תוכנית אנליטיקס

### הבעיה עם 25 אירועים

התוכנית מונה 25 שמות אירועים. **כל אירוע GA4 דורש תג שנוצר ביד ב-GTM.** בלי התג, האירוע מגיע
ל-`dataLayer` ונעצר שם. 25 אירועים = 25 תגים ידניים, וכל אחד שיישכח הוא מדידה שנראית קיימת ואינה.

### ⚠ שני אירועים מהרשימה כבר קיימים, ויפצלו נתון היסטורי

התוכנית עצמה מורה *"לבדוק את שמות האירועים מול סטנדרט האנליטיקס הקיים בפרויקט לפני היישום"*. בדקתי.
`.claude/knowledge/seo-plan.md` §7 מגדיר את ה-Key Events הקיימים ב-GA4:

```
click_call · click_whatsapp · form_submit · click_navigate
```

`click_whatsapp_phone` ו-`click_call_phone` מהתוכנית (פריטים 6 ו-7) הם **אותה פעולה בשם אחר**.
להוסיף אותם פירושו שני אירועים לאותה לחיצה, ופיצול של מדידה שרצה כבר חודשים: דוח "כמה פנו
ב-WhatsApp" יראה חלק מהמספר.

**ההמלצה: לא ליצור אירוע חדש ללחיצת WhatsApp או טלפון.** לחזור להשתמש ב-`click_whatsapp` וב-`click_call`
הקיימים, ולהוסיף להם פרמטר הקשר (`section: phones`, `slug`). זה גם משאיר את ההשוואה ההיסטורית שלמה
וגם חוסך שני תגים.

### ההצעה: 8 אירועים חדשים, ושניים קיימים שמקבלים פרמטר

ב-GA4 פרמטר הוא מימד. אותה יכולת פילוח, שמינית מהתחזוקה, ובלי לפצל נתון קיים:

| אירוע | פרמטרים | מכסה מהרשימה |
|---|---|---|
| `phone_view` | `type` (hub/list/device/brand/guide/compare/upcoming), `slug` | 1, 2, 3, 18, 19 |
| `phone_cta` | `action` (check_price/check_stock), `slug` | 4, 5 |
| *(קיים)* `click_whatsapp` / `click_call` | + `section`, `slug` | 6, 7 |
| `phone_compare` | `step` (add/remove/start/complete), `slugs` | 8, 9, 10, 11 |
| `phone_quiz` | `step` (start/answer/complete/click_result), `q`, `slug` | 12, 13, 14, 15 |
| `phone_import` | `choice` (official/parallel), `slug` | 16, 17 |
| `phone_filter` | `filter`, `value`, `results` | 24, 25 (`results:0` = אין תוצאות) |
| `phone_related` | `kind` (comparison/guide/existing_from_rumor), `from`, `to` | 21, 22, 23 |
| `phone_buy_or_wait` | `slug`, `choice` | 20 |

**ההחלטה שלך.** אם אתה מעדיף את 25 השמות המקוריים אבנה כך, וצריך לדעת שזה 25 תגים.

### מה לא צריך קוד בכלל

**כל CTA יוצא יקבל `?text=` ייחודי.** טריגר קליק גנרי ב-GTM מפלח אותם לפי Click URL בלי שורת קוד
בדף. שבעת ה-CTA במדריך התקלות עובדים כך היום. זה גם מה שמתקן את הבעיה בסעיף 5א.

**קוד רק להתנהגות בתוך הדף** שאף טריגר לא רואה: הוספה להשוואה, שאלון, סינון.

`dataLayer` בטוח בכל סביבה — שומר `PG_PROD` מונע מ-GA4 להיטען מחוץ לפרודקשן, ולכן החיווט נשאר בדיק.

---

## 12א. קופי — תוספת שלא הייתה בגרסה הראשונה

הגרסה הראשונה של התוכנית הזאת **לא כללה מקטע קופי בכלל**, למרות שכללים 15 ו-16 בבריף עוסקים בדיוק
בזה. `.claude/knowledge/04-copywriter.md` הכיל שלושה דברים שמשנים את מה שצריך לכתוב.

### 1. הקופי למכירת מכשירים כבר קיים ואושר

שורה 106 בקובץ הקופירייטר:

> **מכשיר חדש, מסודר עד הסוף** — בוחרים איתכם את המכשיר הנכון (יבוא רשמי או מקביל), מעבירים את כל
> הנתונים מהישן, מגדירים, ומרכיבים את האבזור. **יוצאים עם טלפון שמוכן לעבודה, לא עם קופסה.**

זו העמדה המאושרת של החנות למכירת מכשירים, והיא כבר כתובה. **הכותרת של `/phones/` צריכה להיבנות
עליה ולא להמציא חדשה.** הכותרת שהתוכנית הציעה ("מחפשים טלפון חדש? בואו נמצא את המכשיר שמתאים לכם")
תקינה, אבל "יוצאים עם טלפון שמוכן לעבודה, לא עם קופסה" חזקה ממנה ואינה דורשת אישור.

### 2. שתיים מארבע ההתנגשויות של הלקוח הן הנושא של האזור הזה

שורה 60 מונה את ההתנגדויות שכבר זוהו לפון גת:

| ההתנגדות | מי עונה עליה |
|---|---|
| **"אולי זול יותר ברשת?"** | ⚠ **שום דבר בתוכנית לא עונה על זה** |
| **"יבוא מקביל = פחות אמין?"** | `/guides/official-vs-parallel-import/` |
| "כמה זמן ייקח התיקון?" | עמודי המעבדה הקיימים |
| "יש אחריות?" | אזור היבוא והאחריות |

**ההתנגדות הראשונה היא זו שמכריעה מכירה, והתוכנית לא נותנת לה תשובה.** התשובה קיימת בשורה 106:
מה שקונים בפון גת אינו המכשיר אלא ההעברה, ההגדרה, האבזור, והמעבדה שתטפל בו מחר. `יתרונות הקנייה
בפון גת` במרכז המכשירים צריך להיות **התשובה הזאת במפורש**, ולא רשימת יתרונות גנרית.

ועוד כלל מאותה שורה: **סיגנלי אמון הכי אפקטיביים צמודים ל-CTA.** כלומר היתרונות לא יושבים במקטע
נפרד בתחתית הדף אלא ליד הכפתור.

### 3. הקול של ברוך כבר מוגדר, וזה מה שמצדיק את גשר המעבדה↔מכירה

שורה 100: *"ברוך הוא האיש של המכשירים והמעבדה. אם יש תקלה שאף אחד לא הצליח לפצח, הוא זה שיפרק,
יבדוק, ויגיד לכם את האמת: **מה שווה לתקן ומה לא**."*

הקישור מעמודי התיקון ל-`/phones/` (§5ד) הוא לא רעיון שיווקי שהוספתי — **הוא תפקידו המוצהר של ברוך**.
וזה גם מגדיר את הטון של "הטיפ של ברוך": הוא אומר את האמת, כולל מתי **לא** לקנות.

### שלושה כללי קופי שמשנים רכיבים בתוכנית

| הכלל | מאיפה | מה זה משנה |
|---|---|---|
| **CTA אחד ממוקד למקטע** | שורה 68 | **שורת המכשיר בתוכנית נושאת ארבע פעולות** (לעמוד / להשוואה / בדיקת מחיר / WhatsApp). צריך אחת ראשית והשאר שקטות |
| **מיקרוקופי הרגעה צמוד לכל CTA** | שורה 56 | לתוכנית אין אף שורת מיקרוקופי. תחת "לבדיקת מחיר ומלאי" צריך *"אנחנו עונים בעצמנו, בדרך כלל תוך כמה דקות"* |
| **ביקורת ספציפית גוברת על ממוצע כוכבים** | שורה 64 | 3–5 ביקורות במרכז המכשירים, ו**דווקא כאלה שמדברות על קנייה ולא על תיקון**. ביקורת על החלפת מסך לא עונה להתנגדות של קונה |

### מתיחות אחת שצריכה הכרעה שלך

שורה 16 בקובץ הקופירייטר קובעת: **"אין מכירה אונליין, אין מחירון, אין סל."**
מנגד, מחקר מילות המפתח מזהה **"מחירון"** כביטוי עם ביקוש שאין לו עמוד.

התוכנית נמצאת בין השניים: סינון לפי תקציב ושדה "מחיר או בדיקת מחיר" הם צעד לכיוון מחירון.
**ההמלצה שלי: לשמור על "אין מחירון"** בשלב הראשון (זה גם מה שסעיף 15.4 אומר על מחיר שמתיישן),
ולהחזיק את הביטוי לטווחי מחיר בשירותי המעבדה, לא במכשירים. **אבל זו החלטה שלך.**

---

## 13. המידע שחסר ממך

19 הפריטים מהתוכנית, מסודרים לפי מה שהם **חוסמים**:

### ⛔ חוסם בנייה — בלי זה אין מה לבנות

**1. רשימת המכשירים שנמכרים בפועל.** 10–15 דגמים. אני לא יכול להמציא מה החנות מוכרת, וכל עמוד מכשיר,
כל השוואה, כל סינון וכל תוצאת שאלון נגזרים מהרשימה. **זו הבקשה הראשונה והחשובה ביותר.**

### 🟡 חוסם פרסום, לא בנייה

הדפים ייבנו עם השדות ועם הנוסח החלופי מהתוכנית ("לבדיקת מחיר ומלאי"), אבל לא יעלו לפרודקשן עם
נתון מומצא:

מחירים · צבעים · נפחי אחסון · מצבי מלאי · סוגי יבוא · שמות יבואנים · נותני אחריות · משך אחריות ·
תשלומים · מכשירים בהזמנה · מכשירים שהופסקו · שירות העברת מידע

**מספרי דגם לפי גרסת יבוא** (שדה 33) — זה **לא ניתן לאימות אונליין**. יצרן לא מפרסם איזה מספר דגם
מגיע ליבואן מקביל בישראל. זה חייב להגיע מהחנות או להישאר ריק.

### 🟢 לא חוסם

**סיגל וברוך** — אכין טיוטות מסומנות `status: draft`. הן לא ייכנסו לדף עד אישור, ובדיקת preflight
תאכוף את זה. אין להמציא המלצה בשמם, וטיוטה שמחכה לאישור היא לא המצאה.

**תמונות** — פלייסהולדר `.fig .ph` עם `alt` שנכתב מראש (כלל 5).

**מפרטים** — לא צריך ממך. אני מאמת אונליין מול אתרי היצרנים.

**טלפון, WhatsApp, שעות** — קיימים באתר.

### 🟡 בקשה אחת שנוספה אחרי סקירת הסקילס

**3–5 ביקורות שמדברות על קנייה של מכשיר, לא על תיקון.** קובץ הקופירייטר קובע שביקורת ספציפית גוברת
על ממוצע כוכבים, וש**הלקוח צריך לראות את עצמו בממליץ**. ביקורת על החלפת מסך לא עונה להתנגדות של מי
ששוקל לקנות טלפון. יש 537 ביקורות ב-GBP ו-39 ב-Easy; **צריך לאתר את אלה שנוגעות לקנייה**, או שתגיד
לי אילו לקוחות מוכנים שיצוטטו.

אני יכול לסרוק את הביקורות הקיימות ולסמן מועמדות. **אין לשנות אף מילה בטקסט של ביקורת** (כלל קבוע
בפרויקט), ולכן אם אין ביקורות קנייה, המקטע יישאר בלי ציטוטים ולא עם ציטוט מותאם.

---

## 14. סדר הבנייה

התוכנית מציעה 10 שלבים ו-16 עמודים בגל ראשון. הסדר כאן זהה ברוחו, עם שינוי אחד: **שלב 0 חדש
בהתחלה**, כי בלעדיו כל דף חדש נבנה מחוץ לכל בדיקה (סעיף 15.1).

| שלב | מה | תלוי ב | קלט ממך |
|---|---|---|---|
| **0. תשתית** | הרחבת `preflight.js` לעומק · הרחבת `new-page.js` לכתובות מקוננות · `devices.json` + מחולל · הרכיבים החדשים | — | — |
| **1. מה שלא תלוי בנתונים** | `/guides/official-vs-parallel-import/` · `/phones/` | 0 | — |
| **2. מכשירים** | תבנית · 10–15 עמודים · `/phones/all/` · 3 מותגים | 1 + **רשימת מכשירים** | ⛔ |
| **3. השוואות** | `/compare/` · עמודי השוואה · `/phones/compare/` | 2 | — |
| **4. שאלון** | `/phones/find-my-phone/` | 2 | — |
| **5. מדריכים** | `/guides/` + 7 מדריכים | 2 | — |
| **6. עתידיים** | `/upcoming-phones/` + Apple + Samsung | 1 | — |
| **7. חיבור לאתר** | ניווט · אריח מכירת מכשירים · שקופיות · גשרי מעבדה↔מכירה · sitemap · sw | 2 | — |
| **8. QA סופי** | 8 מסלולי הבדיקה | הכל | — |

### למה להתחיל דווקא ב-0 ואז במדריך היבוא

**שלב 0 חובה.** לא עקיפה.

**מדריך היבוא הוא הדף הראשון הנכון** מארבע סיבות: הוא לא תלוי בשום נתון מסחרי · כל טענה בו ניתנת
לאימות אונליין · הוא הבסיס שתיבת היבוא בכל עמוד מכשיר מפנה אליו · והוא שאלה אמיתית שלקוחות שואלים
בחנות, כלומר תוכן שמצדיק את עצמו גם בלי SEO.

**עוד לא מדובר על הכל בפעם אחת.** 16 עמודים כשכל עמוד מכשיר נושא 35 שדות מאומתים מול אתר יצרן זה
לא סשן אחד. אני מציע לסגור שלב, לעצור, שתסתכל, ואז להמשיך.

---

## 15. נקודות סיכון

### 1. ⛔ preflight עיוור לכל הכתובות של התוכנית

**זה החסם הקונקרטי הראשון וצריך לתקן אותו לפני העמוד הראשון.**

`.claude/preflight.js` (שורות 205–214) סורק את `prototype/` **רמה אחת בלבד**. תיקייה נחשבת עמוד אם
יש בה `index.html`.

התוצאה: `prototype/phones/index.html` יימצא. `prototype/phones/all/index.html`,
`prototype/phones/iphone-17/index.html`, `prototype/compare/x-vs-y/index.html`,
`prototype/guides/…/index.html` ו-`prototype/upcoming-phones/…/index.html` — **לא יימצאו כלל.**

כלומר **רוב האזור החדש יישב מחוץ לכל 20 הבדיקות**: בלי בדיקת שומר סביבה, בלי גידור GTM, בלי canonical,
בלי תפריט נגישות, בלי באנר קוקיז, בלי JSON-LD, בלי בדיקת המקף הארוך.

ההערה בקובץ עצמו (שורות 197–199) מתארת בדיוק את התקלה הזאת בגרסתה הקודמת: *סריקה שטוחה הייתה משאירה
כל עמוד שירות מחוץ לכל הבדיקות, "וזו בדיוק התקלה שבגללה הקובץ הזה נכתב מלכתחילה"*. המבנה החדש עמוק
בשתי רמות, ולכן הבאג חוזר ברמה אחת מתחת.

### 2. ⛔ סקריפט הפיגום דוחה כתובת מקוננת

`new-page.js` שורה 37: `/^[a-z][a-z0-9-]*$/` — **slug עם לוכסן נדחה.** `--slug phones/all` ייפול.
בנוסף, לוגיקת `toRoot` מניחה עומק של רמה אחת בדיוק, כלומר גם אם נעקוף את הבדיקה כל נתיב יחסי במסגרת
יישבר ברמה השנייה.

בלי תיקון הסקריפט, החלופה היא כתיבת HTML ביד — **בדיוק מה ש-§0 בסקיל אוסר**, ומה שיצר את 14 הפערים
במדריך התקלות.

### 3. היקף

16 עמודים, 15 מהם עם 35 שדות מאומתים מול אתרי יצרנים. זו עבודה של שבועות ולא של סשן. הסיכון האמיתי
הוא לא הזמן אלא **ירידת איכות באמצע**: העמודים הראשונים נכתבים היטב והאחרונים מהתבנית.

### 4. מחיר סטטי שמתיישן

מחיר ב-HTML שלא מתעדכן הוא חשיפה צרכנית. **המלצה: להתחיל בלי מחירים בכלל** ("לבדיקת מחיר ומלאי"),
עד שיהיה תהליך עדכון. זו גם הסיבה שאין `Offer`.

### 5. תוכן דק ומשוכפל

15 עמודים מתבנית אחת היא הדרך המהירה ביותר לתוכן דק. לכל מכשיר חייבים להיות **טקסטים שונים באמת**
ב"מה חשוב לדעת", "למי מתאים", "למי פחות מתאים" וההמלצה. מפרט זהה אינו בעיה; פרוזה זהה כן.

### 6. קניבליזציה

`/compare/` מול `/phones/compare/` · `/guides/iphone-or-galaxy/` מול השוואת דגמים · `/phones/all/`
מול עמודי מותג. סעיף 9 קובע היררכיה לכל אחד.

### 7. ניווט של 10 פריטים במובייל

צריך מדידה ב-375px, לא הערכה.

### 8. אירועים שמתים ב-dataLayer

25 אירועים = 25 תגי GTM ידניים. סעיף 12 מציע 8.

### 9. עמודי שמועות דורשים תחזוקה

עמוד שמועות שלא עודכן חצי שנה גרוע מהיעדרו, כי הוא מציג "עודכן לאחרונה" עם תאריך ישן. צריך להחליט
מי מעדכן ובאיזו תדירות **לפני** שבונים אותו.

### 10. סיכון טון AI, הגבוה ביותר בפרויקט הזה

15 עמודי מכשיר מתבנית אחת הם הצורה שמייצרת פרוזה מכונית. רשימת הסימנים ב-§7
(פתיחי bold חוזרים, "לא X אלא Y", תת-כותרות כשאלה, טיקים לשוניים) חייבת לרוץ **על כל עמוד בנפרד**.
רגקס לא תופס את זה, רק קריאה מלאה של הדף.

### 11. CRLF

`prototype/index.html` מערבב סופי שורות ו-`autocrlf=true`. עריכה הופכת ~4000 שורות לדיף. צריך להחזיר
סופי שורות מקוריים לפני קומיט (זיכרון `phonegat-crlf-diff-churn`).

### 12. סשנים במקביל

⛔ ראה בסוף.

### 13. הבידוד של הטקסט הלטיני בטבלאות המפרט

מאה מחרוזות לטיניות בהקשר RTL, ואלגוריתם ה-bidi מסדר אותן מחדש. `iPhone 17 Pro Max` הופך
ל-`Pro Max iPhone 17`, ומידה הופכת סדר ספרות. **הסיכון הוויזואלי הוודאי ביותר באזור הזה**, ואין
רגקס שתופס אותו. הפירוט ב-§6.

### 14. תחרות מול דף הבית על הביטוי המסחרי החשוב ביותר

`קניית מכשיר/טלפון קרית גת` הוא מילת מפתח ראשית מוכרזת, ודף הבית תובע אותה ב-`<title>`. הפירוט
וההמלצה (לעקוב, לא לתקן מראש) ב-§9.

### 15. הצורות שסתרו את מערכת העיצוב

הגרסה הראשונה של רשימת הרכיבים כללה כרטיסים בגריד, תגי גלולה ותיבות מוצלות — שלושה אנטי-פטרנים
מפורשים. תוקן ב-§6. **הסיכון שנשאר: מישהו "יתקן" את זה חזרה**, כי כרטיס עם רדיוס וצל הוא ברירת
המחדל של כל כלי עיצוב. לכן ההכרעה על הצ׳יפים נרשמה במפורש, ומבחן הקבלה המדיד נכנס ל-§16.

---

## 16. תוכנית QA

### לכל עמוד, בסדר הזה

1. `node .claude/preflight.js` — 20+ בדיקות, חוסם
2. דפדפן מקומי, דסקטופ ו-375px
3. **מדידה, לא הסתכלות**: `scrollWidth <= clientWidth` · כל `a,button,summary` ≥44px · יחסי ניגודיות מחושבים · פינוי 70px ל-`nav.mbar`
4. **מבחן הקבלה של מערכת העיצוב** (הסקריפט המדיד מ-`muzli-editorial-design`): `0px` הוא הרדיוס הדומיננטי בפער גדול · `fills` מכיל רק צבעי כפתור, הפס הכהה ולבן · ה-`h1` נפתר לסריף. **`9999px` בכל מקום = הגלולה חזרה**
5. Rich Results Test + Schema Validator
6. **קריאה מלאה של הדף בישיבה אחת** וספירת הצורות מ-§7
7. בדיקת קישורים: אין 404, אין שגיאות Console
8. `curl` עם JS מכובה: ספירת מילים ומספר הרשומות
9. דיווח, ואז **בקשת אישור** לדחיפה לטסטים

### ⚠ שלוש דרישות נגישות שהגרסה הראשונה של התוכנית פספסה

מ-`israeli-accessibility-compliance`, ושלושתן ספציפיות בדיוק לרכיבים החדשים באזור הזה:

**1. הטבלאות — הדפוס המדויק, לא "th scope וזה בסדר".** Step 8 ו-Example 4 בסקיל נותנים את המתכון
המלא: `dir="rtl"` על ה-`<table>` · `<caption>` בעברית (לא `aria-label`) · `scope="col"` ו-`scope="row"`
על תאי הכותרת · **`dir="ltr"` על כל תא שתוכנו לטיני** (מספר דגם, מידה, מחיר) · וסדר ה-DOM חייב
להתאים לסדר הקריאה. טבלת מפרט וטבלת השוואה הן הרכיבים הנגישותיים הרגישים ביותר באזור, ובלי הדפוס
הזה קורא מסך מקריא אותן כרשימת מספרים בלי הקשר.

**2. השאלון הוא טופס, על כל מה שנגזר מזה.** התוכנית מתייחסת אליו כ-UI. מבחינת התקן הוא טופס:
`role="group"` + `aria-labelledby` לכל שאלה · `aria-required="true"` · `aria-describedby` ·
`aria-invalid` · הודעות שגיאה **בעברית** עם `role="alert"` · ו-`dir="ltr"` על שדה מספרי (התקציב).
זה פער אמיתי בתוכנית, לא הידוק.

**3. `aria-live` לכל תוצאה שמשתנה בלי טעינת דף.** הסינון, מגש ההשוואה ותוצאות השאלון משנים תוכן
בלי ניווט. בלי `role="status" aria-live="polite"` שמכריז *"12 מכשירים מתאימים"*, מי שמשתמש בקורא מסך
מסנן ולא שומע דבר. חייב לשבת **מחוץ** לכל אזור שנסגר, אחרת ההכרזה נעלמת איתו.

בנוסף: **בקרה בינארית** ("הציגו רק את ההבדלים") מקבלת `aria-pressed`; **בקרה מחזורית** (סינון עם
כמה מצבים) **לא** מקבלת `aria-pressed` אלא את הערך בתוך השם הנגיש. "לחוץ" בקורא מסך על בקרה עם
שלושה מצבים הוא מידע שגוי.

### ⚠ בדיקה שאף אחד לא היה חושב עליה: הרכיבים החדשים תחת עשרת כללי הנגישות

`pg-new-content-page` דורש את כל עשרת כללי `html.a11y-*` בכל דף תוכן. המשמעות המעשית לאזור הזה:
**טבלת השוואה של 3 עמודות ב-375px תחת `a11y-text-150` תגלוש**, ו-`a11y-contrast-invert` מסנן את כל
הדף כולל הטבלה.

לכן: **כל רכיב חדש נבדק תחת כל עשרת המצבים.** ווידג'ט שמציג בקרה ששוברת את הדף גרוע מהיעדר בקרה,
וזה בדיוק הניסוח שהסקיל משתמש בו.

בנוסף, כלל הדפוס: מפרט טכני הוא בדיוק מה שלקוח מדפיס או שומר כ-PDF לפני שהוא בא לחנות. כללי
`@media print` צריכים לאפס את פילטרי הניגודיות.

### בדיקות preflight חדשות שיש להוסיף

הן הופכות את כללי התוכנית מ"לזכור" ל"אי אפשר לפספס" — בדיוק ההיגיון שבגללו הקובץ קיים:

| בדיקה | מה היא מונעת |
|---|---|
| **סריקה לעומק מלא** | סעיף 15.1. **חייבת להיות ראשונה** |
| עמוד שמועות: אין `Product`/`Offer`/מחיר/מלאי/דירוג | שמועה שמוצגת כמוצר בגוגל |
| עמוד מכשיר: אין `Offer` בלי מחיר שמוצג | הצהרה שגויה לגוגל |
| כל שדה מפרט: יש `spec_source` + `spec_checked` | נתון שאין לו מקור |
| `devices.json` ↔ ה-HTML המחולל מסונכרנים | מפרט סותר בין עמוד להשוואה. אותו היגיון כמו בדיקת הקופונים |
| `recommendation_status != approved` לא בפרודקשן | טיוטה בשם סיגל או ברוך באתר החי |
| כל עמוד מכשיר: תג מלאי אחד מ-6 המצבים | מכשיר בלי מצב מלאי |

### QA סופי לאזור

8 המסלולים מהתוכנית: קוד · עיצוב · תוכן · SEO · מובייל · נגישות · Schema · ביצועים.
ובנוסף מה שהתוכנית מדגישה ואף כלי לא בודק: **התאמה בין ה-Schema למה שהעמוד באמת מציג**, ו**טקסטים
שלא נשמעים כמו AI**.

---

## ⛔ שתי החלטות לפני שנוגעים בקוד

### א. באיזו תיקייה עובדים

זו עבודת "דפים חדשים", כלומר `C:\pg-pages` על הענף `work/pages`. הסשן הזה קשור לתיקיית OneDrive.
בפרויקט הזה נפתחים ארבעה chats שחולקים `.git` אחד, ותיקייה משותפת גרמה כבר לאיבוד עבודה
(`phonegat-worktrees`). אם סשן אחר עובד ב-`pg-pages` — צריך תיקייה נפרדת לאזור הזה.

### ב. מאיפה מתחילים

ההמלצה: **שלב 0 (תשתית) ואז מדריך היבוא**. שלב 0 חובה בכל מקרה, ומדריך היבוא הוא העמוד היחיד
בגל הראשון שאינו תלוי בשום נתון שחסר.

במקביל, הדבר האחד שיאיץ הכל: **רשימת המכשירים שנמכרים בפועל.** ברגע שהיא קיימת, שלב 2 נפתח.

---

## מה הסקילס שינו בתוכנית

גרסה 1 נכתבה מול הקוד. גרסה 2 נכתבה אחרי מעבר על `muzli-editorial-design`,
`israeli-accessibility-compliance`, `knowledge/seo-plan.md` ו-`knowledge/04-copywriter.md`.
**שלושה מהשינויים הם תיקון של משהו שהיה שגוי, לא הידוק.**

| # | מה השתנה | סוג | סעיף |
|---|---|---|---|
| 1 | כרטיסים בגריד, תגי גלולה ותיבות מוצלות היו שלושה אנטי-פטרנים מפורשים. הוחלפו בדפוס הדו-טורי עם קווי שערה, שהוא **המתכון הרשמי של המערכת בדיוק למפרטים ולהשוואות** | **תיקון** | §6 |
| 2 | `click_whatsapp_phone` ו-`click_call_phone` **מכפילים אירועי GA4 קיימים** ומפצלים מדידה שרצה חודשים. חוזרים ל-`click_whatsapp`/`click_call` עם פרמטר | **תיקון** | §12 |
| 3 | `/phones/` מתחרה בדף הבית על `קניית טלפון קרית גת`, מילת מפתח ראשית מוכרזת. לא ראיתי את זה בגרסה 1 | **תיקון** | §9, §15.14 |
| 4 | בידוד LTR לכל מחרוזת לטינית ולכל תא נתונים. הבאג הוויזואלי הוודאי ביותר באזור, ואין רגקס שתופס אותו | תוספת | §6, §15.13 |
| 5 | השאלון הוא **טופס** ולא UI: `role="group"`, `aria-required`, `aria-invalid`, שגיאות בעברית עם `role="alert"` | תוספת | §16 |
| 6 | `aria-live` לסינון, למגש ההשוואה ולתוצאות השאלון. בלעדיו מי שמסנן בקורא מסך לא שומע דבר | תוספת | §16 |
| 7 | דפוס הטבלה הנגישה המלא: `<caption>` בעברית, `scope`, `dir="ltr"` בתאים | תוספת | §16 |
| 8 | בדיקת כל רכיב חדש תחת **עשרת מצבי `a11y-*`**. טבלת 3 עמודות ב-375px תחת `a11y-text-150` תגלוש | תוספת | §16 |
| 9 | מבחן הקבלה המדיד של מערכת העיצוב נכנס לשגרת ה-QA | תוספת | §16 |
| 10 | **מקטע קופי שלם, שלא היה בגרסה 1.** כולל העמדה המאושרת שכבר כתובה ("יוצאים עם טלפון שמוכן לעבודה, לא עם קופסה") | תוספת | §12א |
| 11 | ההתנגדות "אולי זול יותר ברשת?" מזוהה כבר בפרויקט, **ושום דבר בתוכנית לא ענה עליה** | **תיקון** | §12א |
| 12 | שורת המכשיר נשאה ארבע פעולות; הכלל הוא CTA אחד ממוקד למקטע | תיקון | §12א |
| 13 | מיקרוקופי הרגעה צמוד לכל CTA. לגרסה 1 לא היה אף אחד | תוספת | §12א |
| 14 | 3–5 ביקורות **על קנייה** ולא על תיקון, כבקשה נוספת ממך | תוספת | §13 |
| 15 | שתי שאלות פתוחות שנולדו מהסקירה: **יד שנייה** (מילת מפתח קיימת בלי עמוד) ו**מחירון** (הקופי אוסר, ה-SEO מבקש) | שאלה | §9, §12א |

**מה לא שונה:** מבנה הכתובות · מודל הנתונים · סדר הבנייה · שני חסמי התשתית · תוכנית ה-Schema.
הסקילס חיזקו אותם ולא סתרו.

---

*גרסה 1 נכתבה 4.8.2026 מול הקוד. גרסה 2, 5.8.2026, אחרי מעבר על הסקילס. לא לפי הנחות.*
