# אזור המכשירים — תוכנית עבודה

**מצב: מוקפא בכוונה.** האזור הזה מתחיל אחרי שהעמודים הקיימים נסגרים (החלטת אופק, 5.8.2026).
המסמך הזה קיים כדי שאפשר יהיה להרים את העבודה **קר**, בסשן אחר, בלי לגזור מחדש שום החלטה.

ה-*מה ולמה* יושב ב-[`DEVICES-AREA-PLAN.md`](DEVICES-AREA-PLAN.md). כאן רק ה-*מה עושים, באיזה סדר,
ואיך יודעים שמשימה נגמרה*.

---

## איך מרימים את זה קר

```
1. git fetch origin && git log --oneline -5 origin/main      ← מה השתנה מאז 5.8.2026
2. קרא DEVICES-AREA-PLAN.md                                  ← במיוחד §6, §12, §12א, §15, §16
3. קרא CLAUDE.md                                             ← סביבות, אישורים, ענף לכל סשן
4. Skill: pg-new-content-page                                 ← התקן המחייב לכל דף
5. בדוק את לוח המצב למטה                                      ← מה כבר נעשה
```

**התיקייה:** עבודת דפים חדשים שייכת ל-`C:\pg-pages` (ענף `work/pages`, בדיקות ב-`staging-pages`).
אם סשן אחר יושב שם, פותחים תיקייה נפרדת. **לבדוק לפני הפקודה הראשונה של גיט** — סשן יכול להיפתח
בתיקיית הניהול (`…\.git\worktrees\…`) במקום ב-worktree עצמו.

**שני כללים שלא נמשכים קדימה:**
1. **אף דחיפה בלי אישור מפורש של אופק לשינוי הזה** — גם לא לטסטים. מסיימים, מריצים preflight,
   בודקים מקומית, מקמטים, **עוצרים ומדווחים**. הפקודה מוצעת לו, לא מורצת.
2. `git commit -am` סוחף עבודה של סשן אחר. `git add <הקבצים שלך>` ואז `commit` בלי `-a`.

---

## לוח מצב

| שלב | מצב | חוסם |
|---|---|---|
| תכנון | ✅ הושלם 5.8.2026 | — |
| D0 — תשתית | ⬜ לא התחיל | — |
| D1 — מדריך היבוא + מרכז | ⬜ לא התחיל | D0 |
| D2 — מכשירים | ⬜ לא התחיל | D1 + **רשימת מכשירים מאופק** |
| D3 — השוואות | ⬜ לא התחיל | D2 |
| D4 — שאלון | ⬜ לא התחיל | D2 |
| D5 — מדריכים | ⬜ לא התחיל | D2 |
| D6 — עתידיים | ⬜ לא התחיל | D1 |
| D7 — חיבור לאתר | ⬜ לא התחיל | D2 |
| D8 — QA סופי | ⬜ לא התחיל | הכל |

**קלט אחד חוסם הכל מ-D2 והלאה: רשימת 10–15 המכשירים שנמכרים בפועל.**
כל השאר (מפרטים, תמונות, נתונים מסחריים) לא חוסם בנייה. הפירוט ב-§13 בתוכנית.

---

## D0 — תשתית

**בלי השלב הזה כל דף באזור נבנה מחוץ לכל בדיקה.** לא עקיפה. הפירוט ב-§15.1 ו-§15.2 בתוכנית.

| # | משימה | קבצים | גמור כאשר |
|---|---|---|---|
| **D0.1** | הרחבת `preflight.js` לסריקה רקורסיבית | `.claude/preflight.js:205-214` | תיקייה זמנית `prototype/x/y/index.html` **נתפסת** בבדיקות. הבדיקה: לשבור בה משהו במכוון (למחוק `alt`) ולראות כשל |
| **D0.2** | `new-page.js` מקבל slug מקונן | `.claude/skills/pg-new-content-page/scripts/new-page.js:37` + לוגיקת `toRoot` | `--slug phones/all` מייצר דף שכל נתיביו נפתרים בשתי רמות עומק. לבדוק ב-`file://` |
| **D0.3** | סכמת `devices.json` | `prototype/devices.json` | 54 השדות מ-§8, **`spec_source` ו-`spec_checked` לכל שדה מפרט** ולא לכל מכשיר. `recommendation_status` קיים |
| **D0.4** | סקריפט המחולל | `.claude/tools/gen-devices.js` | קורא `devices.json`, כותב HTML סטטי + `devices-public.json`. **טיוטת המלצה לא נכנסת ל-HTML** |
| **D0.5** | הגנה על `devices.json` בפרודקשן | `prototype/vercel.json` | `/devices.json` מפנה כמו `services.json`. `/devices-public.json` נגיש |
| **D0.6** | הרכיבים החדשים ב-CSS | הדף הראשון | **13 הרכיבים מ-§6, בצורות המתוקנות.** מבחן הקבלה של מערכת העיצוב עובר: `0px` דומיננטי, `fills` נקי, `h1` סריף |
| **D0.7** | בידוד LTR | אותו מקום | מחלקת `.ltr` / `<bdo>` על כל מחרוזת לטינית ותא נתונים. **הבדיקה היא הסתכלות בעין, אין רגקס** |
| **D0.8** | בדיקות preflight חדשות | `.claude/preflight.js` | 7 הבדיקות מ-§16: אין `Offer` בלי מחיר · אין `Product` בעמוד שמועות · מקור לכל שדה מפרט · `devices.json` ↔ HTML מסונכרנים · `status != approved` לא בפרודקשן · מצב מלאי קיים |

⛔ **שער: D0.1 ו-D0.2 חייבים לעבור לפני שנוצר דף אחד.**

---

## D1 — מדריך היבוא והמרכז

שני העמודים היחידים בגל הראשון שאינם תלויים בשום נתון חסר.

| # | משימה | גמור כאשר |
|---|---|---|
| **D1.1** | מחקר אונליין: יבוא רשמי מול מקביל | לכל טענה יש מקור ותאריך. **אין ייעוץ משפטי.** אין "רשמי תמיד יותר טוב" ואין "מקביל תמיד יותר זול" |
| **D1.2** | `/guides/official-vs-parallel-import/` | 14 המקטעים מהתוכנית. `Article` + `BreadcrumbList` |
| **D1.3** | `/guides/` מרכז המדריכים | breadcrumb תקין. מקשר גם ל-`/phone-problems/` הקיים |
| **D1.4** | `/phones/` מרכז המכשירים | **שלושה מסלולים** (צפייה / השוואה / עזרה). השאלון **לא** נפתח לבד. הכותרת על בסיס הקופי המאושר (§12א). `יתרונות הקנייה` עונה על "אולי זול יותר ברשת?" וצמוד ל-CTA |
| **D1.5** | QA לשני העמודים | 9 שלבי §16, כולל קריאה מלאה וספירת סימני AI |

🔵 **נקודת אישור 1 — אופק רואה את מדריך היבוא ואת המרכז בטסטים.** הפקודה מוצעת, לא מורצת.

---

## D2 — מכשירים

⛔ **חוסם: רשימת המכשירים.**

| # | משימה | גמור כאשר |
|---|---|---|
| **D2.1** | קליטת רשימת המכשירים | 10–15 דגמים ב-`devices.json` עם slug וסטטוס |
| **D2.2** | אימות מפרטים אונליין | **אתר היצרן קודם.** מקור ותאריך לכל שדה. מספר דגם לפי יבוא **נשאר ריק** אם לא הגיע מהחנות |
| **D2.3** | תבנית עמוד מכשיר | כל מקטעי §7 בתוכנית. `Product` + `Brand` + `BreadcrumbList`, **בלי `Offer`** |
| **D2.4** | 10–15 עמודי מכשיר | **פרוזה שונה באמת** ב"מה חשוב לדעת", "למי מתאים", "למי פחות מתאים". מפרט זהה מותר, פרוזה זהה לא |
| **D2.5** | טיוטות סיגל וברוך | `status: draft`, שונה לכל מכשיר. **לא מופיע בדף עד אישור** |
| **D2.6** | `/phones/all/` | כל השורות ב-HTML. סינון מסתיר בצד לקוח בלבד. `aria-live` על התוצאה. בדיקת `curl` בלי JS |
| **D2.7** | 3 עמודי מותג | התוכן הספציפי לכל מותג לפי §6 בתוכנית |
| **D2.8** | QA | + כל רכיב תחת עשרת מצבי `a11y-*` |

🔵 **נקודת אישור 2.**

---

## D3–D6 — השוואות, שאלון, מדריכים, עתידיים

| # | משימה | הערה מכריעה |
|---|---|---|
| **D3.1** | `/phones/compare/` הכלי | **`noindex` + canonical ל-`/compare/`.** מגש דביק, פינוי 70px |
| **D3.2** | טבלת ההשוואה | ⚠ **החלטת העיצוב מ-§6 טרם התקבלה**: 3 עמודות ב-375px. להכריע לפני הבנייה |
| **D3.3** | `/compare/` + עמודי השוואה | לפי מחקר מילות מפתח והמלאי, לא לפי הדוגמאות בתוכנית. **אין הכרזת מנצח** |
| **D4.1** | `/phones/find-my-phone/` | **טופס לכל דבר** (§16): `role="group"`, `aria-required`, שגיאות בעברית, `dir="ltr"` על התקציב |
| **D4.2** | לוגיקת השאלון | ההמלצות **מהמאגר בלבד.** תוצאה = "שלושה מכשירים שכדאי לבדוק", לא "המכשיר המושלם" |
| **D4.3** | תוכן סטטי בשאלון | בלי זה העמוד רזה. הסבר שעומד בפני עצמו |
| **D5.1** | מחקר מילות מפתח ל-7 המדריכים | **אין נפחים מומצאים.** בלי נתון אמיתי = מועמד למחקר |
| **D5.2** | 7 מדריכי קנייה | אין "הטלפון הטוב בישראל". התאמה לפי צורך ותקציב |
| **D6.1** | `/upcoming-phones/` | ⚠ **להחליט מי מעדכן ובאיזו תדירות לפני הבנייה.** עמוד שמועות לא מעודכן גרוע מהיעדרו |
| **D6.2** | 2 עמודי שמועות | 4 רמות אמינות. **אין `Product`, `Offer`, מחיר, מלאי, דירוג.** אין תאריך השקה מומצא |

🔵 **נקודת אישור אחרי כל שלב.**

---

## D7 — חיבור לאתר הקיים

**רק אחרי ש-D2 באוויר.** אין להפנות כפתור לדף שלא עובד.

| # | משימה | קובץ | גמור כאשר |
|---|---|---|---|
| **D7.1** | אריח "מכירת מכשירים" | `prototype/index.html` › `#services` | כפתור ראשי ל-`/phones/`, WhatsApp נשאר ירוק עם **prefill ייחודי** |
| **D7.2** | שקופית "מכשירים חדשים" | `#deals` | כפתור **נוסף** ל-`/phones/`. ה-WhatsApp הקיים לא נוגעים בו |
| **D7.3** | פריט ניווט `מכשירים` | 10 הדפים + החדשים | **מדידה ב-375px** שעשרה פריטים לא גולשים |
| **D7.4** | גשר מעבדה↔מכירה | 5 עמודי שירות + המדריך | **בהקשר, לא באנר.** זה תפקידו המוצהר של ברוך (§12א) |
| **D7.5** | `sitemap.xml` | | רק בהשקה לפרודקשן |
| **D7.6** | `sw.js` | | כתובות + **החלפת שם המטמון** `pg-v4`→`pg-v5` |
| **D7.7** | `services.json` | | הדפים החדשים רשומים לפאנל הסקירה |
| **D7.8** | `accessibility.html` | | **שני התאריכים.** preflight נכשל אם תאריך הביקורת מקדים את הדף החדש |
| **D7.9** | GTM | | 8 התגים החדשים. `click_whatsapp`/`click_call` **מקבלים פרמטר, לא מוחלפים** |
| **D7.10** | Search Console | | הגשה מחדש של הסייטמאפ |

⚠ **D7.1–D7.4 נוגעים ב-`index.html`.** מערבב סופי שורות + `autocrlf=true` = דיף של ~4000 שורות.
להחזיר סופי שורות מקוריים לפני קומיט.

---

## D8 — QA סופי

8 מסלולי §16 על כל האזור, ובנוסף השניים שאף כלי לא בודק:

1. **התאמה בין ה-Schema למה שהעמוד באמת מציג.**
2. **קריאת כל עמוד בישיבה אחת וספירת הצורות** מ-`pg-new-content-page` §7. 15 עמודי מכשיר מתבנית
   אחת הם הצורה שמייצרת פרוזה מכונית, וזה הסיכון האיכותי הגבוה ביותר באזור.

---

## ההחלטות שממתינות לאופק

| # | ההחלטה | למה עכשיו |
|---|---|---|
| 1 | **רשימת 10–15 המכשירים** | חוסם את D2 והלאה |
| 2 | התיקייה: `C:\pg-pages` או חדשה | לפני הקומיט הראשון |
| 3 | 8 אירועים עם פרמטרים, או 25 שמות | 25 = 25 תגי GTM ידניים |
| 4 | **יד שנייה באזור?** | משנה את מודל הנתונים, לכן לפני D0.3 |
| 5 | **מחירון?** הקופי אוסר, ה-SEO מבקש | משפיע על שדה המחיר ועל הסינון |
| 6 | 3 עמודות בהשוואה ב-375px | לפני D3.2 |
| 7 | מי מתחזק את עמודי השמועות | לפני D6.1 |
| 8 | ביקורות על **קנייה** | ל-D1.4 |

---

*נכתב 5.8.2026 על בסיס `DEVICES-AREA-PLAN.md` גרסה 2. טרם נכתבה שורת קוד אחת.*
