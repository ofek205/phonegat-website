# אזור המכשירים — תוכנית עבודה

**מצב: בעבודה.** אופק אישר להתחיל ב-5.8.2026 ("אין סשן אחר, אתה יכול להתחיל"), ולכן שאלת
התיקייה נסגרה: עובדים בתיקיית OneDrive על ענף `devices-area`.
המסמך הזה קיים כדי שאפשר יהיה להרים את העבודה **קר**, בסשן אחר, בלי לגזור מחדש שום החלטה.

ה-*מה ולמה* יושב ב-[`DEVICES-AREA-PLAN.md`](DEVICES-AREA-PLAN.md). כאן רק ה-*מה עושים, באיזה סדר,
ואיך יודעים שמשימה נגמרה*.

---

## איך מרימים את זה קר

```
1. git fetch origin && git log --oneline -5 origin/main      ← מה השתנה מאז 5.8.2026
2. קרא DEVICES-AREA-PLAN.md                                  ← במיוחד §6, §12, §12א, §15, §16
3. קרא CLAUDE.md                                             ← סביבות, אישורים, ענף לכל סשן
4. Skill: pg-new-content-page                                 ← התקן המחייב לכל דף
5. בדוק את לוח המצב למטה                                      ← מה כבר נעשה
```

**התיקייה:** עבודת דפים חדשים שייכת ל-`C:\pg-pages` (ענף `work/pages`, בדיקות ב-`staging-pages`).
אם סשן אחר יושב שם, פותחים תיקייה נפרדת. **לבדוק לפני הפקודה הראשונה של גיט** — סשן יכול להיפתח
בתיקיית הניהול (`…\.git\worktrees\…`) במקום ב-worktree עצמו.

**שני כללים שלא נמשכים קדימה:**
1. **אף דחיפה בלי אישור מפורש של אופק לשינוי הזה** — גם לא לטסטים. מסיימים, מריצים preflight,
   בודקים מקומית, מקמטים, **עוצרים ומדווחים**. הפקודה מוצעת לו, לא מורצת.
2. `git commit -am` סוחף עבודה של סשן אחר. `git add <הקבצים שלך>` ואז `commit` בלי `-a`.

---

## לוח מצב

| שלב | מצב | חוסם |
|---|---|---|
| תכנון | ✅ הושלם 5.8.2026 | — |
| **D0 — תשתית** | ✅ **הושלם.** החלטה 4 נענתה: אין יד שנייה | — |
| D1 — מדריך היבוא + מרכז | ✅ הושלם 5.8.2026 | — |
| **D2 — מכשירים** | ✅ **12 עמודים באוויר**, כולם ב-`status: review` עד שהנתונים המסחריים מגיעים | — |
| **D3 — השוואות** | ✅ **הושלם.** 8 עמודי השוואה, מרכז, והכלי האינטראקטיבי | — |
| **D4 — שאלון** | ✅ **הושלם.** `/phones/find-my-phone/`, 7 שאלות | — |
| **D5 — מדריכים** | 🟡 **D5.1 הושלם. מדריך 1 מ-7 באוויר** (eSIM) | — |
| D6 — עתידיים | ⬜ לא התחיל | החלטה 7 (מי מתחזק) |
| **D7 — חיבור לאתר** | 🟡 **D7.1–D7.4, D7.6, D7.7 הושלמו.** נשארו רק פריטי השקה: סייטמאפ, תאריכי הנגישות, GTM, Search Console | אישור העלאה |
| D8 — QA סופי | ⬜ לא התחיל | הכל |

**24 עמודים באזור:** 2 מדריכים · מרכז המכשירים · 12 עמודי מכשיר · מרכז ההשוואות · 8 עמודי
השוואה · כלי ההשוואה · השאלון. כולם מקושרים מהתפריט העליון ומדף הבית.

### 12 עמודי המכשיר, ומה חסר בכל אחד

הסדר הוא הסדר שמוצג ב-`/phones/`, והוא **ממוין ב-`gen-devices.js`** ולא לפי הסדר ב-JSON.

| דגם | מקור המפרט | איך נקרא |
|---|---|---|
| iPhone 17 Pro Max · 17 Pro · 17 · 17e · 16 | `apple.com/il` | `fetch` עובד |
| Galaxy S26 Ultra · S26 · A56 5G · A36 5G | `samsung.com/il` | **דפדפן.** המפרט מרונדר ב-JS |
| Xiaomi 15 · Redmi Note 14 Pro · Redmi Note 14 | `mi.com/global` | **דפדפן.** חוסם `fetch`, ואין עמוד ישראלי |

**חסר בכל 12 העמודים, וזה קלט של אופק:** 15 שדות מסחריים (מחיר, מלאי, נפחים וצבעים במלאי,
יבואן, מי חתום על האחריות, כמה חודשים, תנאי שירות, אמצעי תשלום, העברת נתונים), שני טקסטי
המלצה של סיגל וברוך, ותמונות. **שדה ריק אינו מוצג כאפס אלא כשאלה** בטבלת "מה שצריך לברר
לפני קנייה", ולכן העמודים תקינים לסקירה כמו שהם.

**שלוש תגליות שהאימות העלה, ומחכות להכרעה** (רשומות ב-`_candidates_findings` ב-`devices.json`,
והרשימה ב-`_candidates` עצמה לא שונתה):
1. **iPhone 16e אינו נמכר יותר.** נבנה `iPhone 17e` במקומו, שהוא היורש בקטלוג.
2. **iPhone Air, Galaxy S26+, Xiaomi 15 Ultra** נמצאים בקטלוג ואינם ברשימה.
3. **Galaxy A57 ו-A37** הם הדור החדש של סדרת A. A56 ו-A36 עוד נמכרים ולכן נבנו.
   שאלה: מה מהדור הקודם עוד במלאי.

---

## D0 — תשתית

**בלי השלב הזה כל דף באזור נבנה מחוץ לכל בדיקה.** לא עקיפה. הפירוט ב-§15.1 ו-§15.2 בתוכנית.

| # | משימה | קבצים | גמור כאשר |
|---|---|---|---|
| **D0.1** ✅ | הרחבת `preflight.js` לסריקה רקורסיבית | `.claude/preflight.js` | **עבר.** עמוד נשתל ב-`pgtest/deep/` עם GTM לא מגודר ⇒ 7 כשלים ו-6 אזהרות, כל אחד נוקב בנתיב. לפני: אפס. שאר העץ לא זז |
| **D0.2** ✅ | `new-page.js` מקבל slug מקונן | `new-page.js` | **עבר.** `--slug pgtest/deep-scaffold` מייצר דף שעובר preflight בלי אף כשל, `canonical` נכון לעומק שתיים, `register('/sw.js')` שורשי, מעטפת ה-sw קיבלה את הכתובת. `phones/../evil` נדחה. **`toRoot` לא נגעתי, היא הייתה נכונה מלכתחילה** (שורשי־מוחלט, לא `../`) |
| **D0.3** | סכמת `devices.json` | `prototype/devices.json` | 54 השדות מ-§8, **`spec_source` ו-`spec_checked` לכל שדה מפרט** ולא לכל מכשיר. `recommendation_status` קיים |
| **D0.4** | סקריפט המחולל | `.claude/tools/gen-devices.js` | קורא `devices.json`, כותב HTML סטטי + `devices-public.json`. **טיוטת המלצה לא נכנסת ל-HTML** |
| **D0.5** | הגנה על `devices.json` בפרודקשן | `prototype/vercel.json` | `/devices.json` מפנה כמו `services.json`. `/devices-public.json` נגיש |
| **D0.6** | הרכיבים החדשים ב-CSS | הדף הראשון | **13 הרכיבים מ-§6, בצורות המתוקנות.** מבחן הקבלה של מערכת העיצוב עובר: `0px` דומיננטי, `fills` נקי, `h1` סריף |
| **D0.7** | בידוד LTR | אותו מקום | מחלקת `.ltr` / `<bdo>` על כל מחרוזת לטינית ותא נתונים. **הבדיקה היא הסתכלות בעין, אין רגקס** |
| **D0.8** | בדיקות preflight חדשות | `.claude/preflight.js` | 7 הבדיקות מ-§16: אין `Offer` בלי מחיר · אין `Product` בעמוד שמועות · מקור לכל שדה מפרט · `devices.json` ↔ HTML מסונכרנים · `status != approved` לא בפרודקשן · מצב מלאי קיים |

✅ **השער עבר 5.8.2026.** אפשר לייצר דפים מקוננים, והם נבדקים.

**מה שנמצא בדרך ולא היה בתוכנית:** פירורי הלחם. הפיגום מעתיק שתי רמות (בית › הדף) ומחליף את
השנייה. עמוד בעומק שתיים צריך שלוש, ואת השם העברי של ההורה הסקריפט לא יכול לדעת. הוא עכשיו
**אומר את זה בפלט** במקום להמציא, ולכן זו משימה של מי שבונה את הדף ולא של הסקריפט.

⛔ **D0.3 עד D0.5 חוסמים על החלטה 4 (יד שנייה).** מודל הנתונים הוא הדבר שהכי יקר לשנות אחרי
שכתבו אליו, ומכשיר יד שנייה מוסיף לו מצב, אחריות אחרת ומקור מחיר אחר. **אין להתחיל את
`devices.json` לפני התשובה.**

D0.6 (הרכיבים ב-CSS) ו-D0.7 (בידוד LTR) נולדים בתוך הדף הראשון, כלומר ב-D1, ולא לפניו.
D0.8 (הבדיקות החדשות) מתחלק: הבדיקות שנוגעות ל-`devices.json` חוסמות איתו, ושתיים שאינן
(אין `Product` בעמוד שמועות, אין `Offer` בלי מחיר) אפשר לכתוב מתי שנוח.

---

## D1 — מדריך היבוא והמרכז

שני העמודים היחידים בגל הראשון שאינם תלויים בשום נתון חסר.

| # | משימה | גמור כאשר |
|---|---|---|
| **D1.1** ✅ | מחקר אונליין: יבוא רשמי מול מקביל | **עבר.** הכל ב-`content/import-guide-sources.md` עם מקור ותאריך, כולל מה שלא אומת ולכן לא נכתב |
| **D1.2** ✅ | `/guides/official-vs-parallel-import/` | **עבר.** 1,540 מילים קריאות עם JS מכובה · `Article` + `BreadcrumbList` + `FAQPage` (5 שאלות, תואם לדף) + `#business` · preflight אפס כשלים · מבחן העיצוב `0px`×189 מול `4px`×5 · אפס גלישה ב-375px · כל היעדים ≥44px · ניגודיות מינימלית 5.59:1 · **11 מצבי `a11y-*` נבדקו, כולל טקסט 150% עם ריווח 2.0** |
| **D1.3** ✅ | `/guides/` מרכז המדריכים | **עבר.** `CollectionPage` + `ItemList` + פירור שלוש רמות במדריך היבוא + קישור חוזר. רכיב חדש `.hub`: שורות בקו שערה עם מספר סריף ב-`::before`, כלומר קורא מסך קורא את השם ולא "01שם". אפס קישורים שבורים מתוך 12 |
| **D1.3א** ✅ | חיבור מהאתר הקיים | **עבר.** תשובת ה-FAQ בדף הבית מקשרת למדריך היבוא. דרש כלל CSS, כי `a{color:inherit;text-decoration:none}` הפך אותו לבלתי נראה כקישור. נמדד 6.9:1 עם קו תחתון. דיף של 5 שורות, CRLF נשמר |
| **D1.4** ⛔ | `/phones/` מרכז המכשירים | **מוחזק בכוונה.** שלושת המסלולים שלו (`all/`, `compare/`, `find-my-phone/`) מצביעים לעמודים שאינם קיימים, כלומר העמוד היה עולה עם שלושה כפתורים מתים, והתפקיד המרכזי שלו דורש את רשימת המכשירים. **נבנה עם D2** |
| **D1.5** | QA לשני העמודים | ✅ בוצע לשני העמודים שקיימים. חוזר על `/phones/` כשייבנה |

🔵 **נקודת אישור 1 — אופק רואה את מדריך היבוא ואת המרכז בטסטים.** הפקודה מוצעת, לא מורצת.

---

## D2 — מכשירים

⛔ **חוסם: רשימת המכשירים.**

| # | משימה | גמור כאשר |
|---|---|---|
| **D2.1** | קליטת רשימת המכשירים | 10–15 דגמים ב-`devices.json` עם slug וסטטוס |
| **D2.2** | אימות מפרטים אונליין | **אתר היצרן קודם.** מקור ותאריך לכל שדה. מספר דגם לפי יבוא **נשאר ריק** אם לא הגיע מהחנות |
| **D2.3** | תבנית עמוד מכשיר | כל מקטעי §7 בתוכנית. `Product` + `Brand` + `BreadcrumbList`, **בלי `Offer`** |
| **D2.4** | 10–15 עמודי מכשיר | **פרוזה שונה באמת** ב"מה חשוב לדעת", "למי מתאים", "למי פחות מתאים". מפרט זהה מותר, פרוזה זהה לא |
| **D2.5** | טיוטות סיגל וברוך | `status: draft`, שונה לכל מכשיר. **לא מופיע בדף עד אישור** |
| **D2.6** | `/phones/all/` | כל השורות ב-HTML. סינון מסתיר בצד לקוח בלבד. `aria-live` על התוצאה. בדיקת `curl` בלי JS |
| **D2.7** | 3 עמודי מותג | התוכן הספציפי לכל מותג לפי §6 בתוכנית |
| **D2.8** | QA | + כל רכיב תחת עשרת מצבי `a11y-*` |

🔵 **נקודת אישור 2.**

---

## D3–D6 — השוואות, שאלון, מדריכים, עתידיים

| # | משימה | הערה מכריעה |
|---|---|---|
| **D3.1** | `/phones/compare/` הכלי | ✅ **התוכנית טעתה בשני פרטים כאן.** ראה למטה |
| **D3.2** | טבלת ההשוואה | ✅ **החלטה 6 הוכרעה במדידה ולא בטעם.** ראה למטה |
| **D3.3** | `/compare/` + עמודי השוואה | ✅ 8 זוגות + מרכז. אין הכרזת מנצח ואין מחיר בשום עמוד |

### החלטה 6, ואיך היא הוכרעה

התוכנית הציעה טבלת השוואה עם **עמודה לכל מכשיר**. נמדד ב-375px, מכל של 351px, עם מחרוזות
המפרט האמיתיות מ-`devices.json`:

| צורה | עמודת ערך | התא הגרוע |
|---|---|---|
| שלוש עמודות זו לצד זו | **76px** | **16 שורות** |
| שתי עמודות זו לצד זו | 91px | 9 שורות |
| **מכשיר כשורה, שדה כקטגוריה** | **215px** | **5 שורות** |

טבלה ברוחב התוכן המלא הייתה דורשת **2162px**, כלומר שש מסכים של גלילה הצידה.

**המסקנה: עם מחרוזות מפרט בעברית, עמודה לכל מכשיר לא עובדת בטלפון. גם לא שתיים.** התוכן כאן
הוא משפטים ולא מספרים קצרים. לכן כל שדה הוא `tbody` עם כותרת שמשתרעת, ובתוכו שורה לכל
מכשיר, כלומר **שתי עמודות תמיד**. הצורה הזאת גם לא נשברת כשמשווים שלושה או ארבעה דגמים, כי
מכשיר הוא שורה ולא עמודה, וגם נשארת `<table>` אמיתית עם `scope` נכון בכל מצב, בלי הפטרן של
`display:block` שמוחק את סמנטיקת הטבלה לקוראי מסך.

**הטבלה מציגה רק שדות שבהם שני הדגמים שונים**, ואומרת כמה שדות זהים. השוואה שמציגה 27 שדות
שמתוכם 20 זהים היא גיליון מפרט כפול ולא השוואה.

### D3.1 — הכלי, ושתי טעויות בתוכנית

**התוכנית ביקשה `canonical` ל-`/compare/` יחד עם `noindex`. בדיקה 6 ב-`preflight` פסלה את זה,
ובצדק:** אלה שני סיגנלים סותרים. ה-`canonical` אומר "אנדקס את הכתובת ההיא במקום", וה-`noindex`
אומר "אל תאנדקס בכלל", וגוגל ממליץ במפורש לא לשלב. השילוב היה מתאים אם הכלי היה עותק של
`/compare/`, והוא לא: שם רשימה של שמונה השוואות מוכנות, וכאן בורר. **תוכן אחר שלא רוצים לאנדקס,
ולכן `noindex,follow` עם `canonical` לעצמו.**

**התוכנית ביקשה "מגש דביק, פינוי 70px".** מגש `fixed` היה מתנגש בסרגל המובייל וזו הסיבה
לפינוי. במקום זה שורת הסיכום היא `position:sticky` מתחת לכותרת האתר (66px), כלומר **אין
התנגשות ואין צורך בפינוי בכלל.** אותה תועלת, בלי הבעיה שהפינוי בא לפתור.

**מה שנמנע כאן, וזה העיקר: עותק שני של האלגוריתם.** חישוב ההבדלים חי ב-`diffSpec`, ב-Node.
כלי שמריץ אותו חישוב בדפדפן היה עותק שני, והוא היה הגרוע מכולם כי הוא **בלתי נראה**: הכלי היה
מציג הבדל שהעמוד הקבוע לא מציג, ואף בדיקה לא הייתה תופסת.

לכן המחולל מחשב מראש את כל **66 הזוגות** ומטמיע רק את **שמות השדות שנמצאו שונים**. הערכים
נשלפים מ-`devices.json` בזמן ריצה. שלושה מכשירים עובדים בלי חישוב חדש, כי שדה שונה בין שלושה
אם ורק אם הוא שונה באחד הזוגות: **איחוד קבוצות, לא אלגוריתם.** זה עובד רק מפני שהפריסה היא
מכשיר-כשורה, ועמודה לכל מכשיר הייתה נשברת בשלושה.

**אומת מול כל שמונת העמודים הקבועים:** הכלי והעמוד מדווחים אותם מספרים בדיוק, בכל שמונה
הזוגות, גם בשדות השונים וגם בזהים.
| **D4.1** | `/phones/find-my-phone/` | ✅ `fieldset`+`legend` ולא `role="group"`, ראה למטה. `aria-required`, שגיאות בעברית עם `role="alert"`, `dir="ltr"` על התקציב |
| **D4.2** | לוגיקת השאלון | ✅ התכונות נגזרות במחולל ונופלות בקול, הניקוד בדפדפן. שלוש תוצאות עם נימוק לכל אחת |
| **D4.3** | תוכן סטטי בשאלון | ✅ "איך אנחנו בוחרים מכשיר בחנות", כולל שתי השאלות שהשאלון לא ישאל |
| **D5.1** | מחקר מילות מפתח ל-7 המדריכים | **אין נפחים מומצאים.** בלי נתון אמיתי = מועמד למחקר |
| **D5.2** | 7 מדריכי קנייה | אין "הטלפון הטוב בישראל". התאמה לפי צורך ותקציב |
| **D6.1** | `/upcoming-phones/` | ⚠ **להחליט מי מעדכן ובאיזו תדירות לפני הבנייה.** עמוד שמועות לא מעודכן גרוע מהיעדרו |
| **D6.2** | 2 עמודי שמועות | 4 רמות אמינות. **אין `Product`, `Offer`, מחיר, מלאי, דירוג.** אין תאריך השקה מומצא |

🔵 **נקודת אישור אחרי כל שלב.**

---

## D7 — חיבור לאתר הקיים

**רק אחרי ש-D2 באוויר.** אין להפנות כפתור לדף שלא עובד.

| # | משימה | קובץ | גמור כאשר |
|---|---|---|---|
| **D7.1** | אריח "מכירת מכשירים" | `prototype/index.html` › `#services` | כפתור ראשי ל-`/phones/`, WhatsApp נשאר ירוק עם **prefill ייחודי** |
| **D7.2** | שקופית "מכשירים חדשים" | `#deals` | כפתור **נוסף** ל-`/phones/`. ה-WhatsApp הקיים לא נוגעים בו |
| **D7.3** | פריט ניווט `מכשירים` | 10 הדפים + החדשים | **מדידה ב-375px** שעשרה פריטים לא גולשים |
| **D7.4** | גשר מעבדה↔מכירה | 5 עמודי שירות + המדריך | **בהקשר, לא באנר.** זה תפקידו המוצהר של ברוך (§12א) |
| **D7.5** | `sitemap.xml` | | רק בהשקה לפרודקשן |
| **D7.6** | `sw.js` | ✅ | `gen-devices.js` מוסיף כתובת **ומעלה את שם המטמון בעצמו**. היה ידני עד `pg-v8`, ונשכח |
| **D7.7** | `services.json` | ✅ | `gen-devices.js` רושם כל עמוד מכשיר לפאנל הסקירה בעצמו |
| **D7.8** | `accessibility.html` | | **שני התאריכים.** preflight נכשל אם תאריך הביקורת מקדים את הדף החדש |
| **D7.9** | GTM | | 8 התגים החדשים. `click_whatsapp`/`click_call` **מקבלים פרמטר, לא מוחלפים** |
| **D7.10** | Search Console | | הגשה מחדש של הסייטמאפ |

⚠ **D7.1–D7.4 נוגעים ב-`index.html`.** מערבב סופי שורות + `autocrlf=true` = דיף של ~4000 שורות.
להחזיר סופי שורות מקוריים לפני קומיט.

---

## הפגם שנסגר, ואיך אותרו 34 הפיקסלים

`/guides/esim-israel/` גלש 34px ב-375px תחת `a11y-text-150` בלבד. **הגורם: כפתור ההירו.**

שני חשדות ראשונים נשללו, ושניהם היו סבירים:

| חשד | איך נשלל |
|---|---|
| הטבלה `.cmp` | `guides/how-much-storage/` נבנה עם **שתי** טבלאות מאותו מקור, ונקי לגמרי |
| בלוק ה-`.warn` | הוסר בדפדפן, והגלישה נשארה 34px |
| `min-width:auto` בפריט flex | `min-inline-size:0` על שרשרת ההורים לא שינה דבר, וה-CSS הוסר |

**מה שאיתר:** הסרת אלמנטים אחד אחד, בדפדפן, בלי לגעת בקובץ. מקטע ה-`ghero` הפיל את הגלישה
לאפס, ובתוכו `.hcta` בלבד. תווית הכפתור "שאלו אותנו על מעבר ל-eSIM" ארוכה, ותחת הגדלת טקסט
היא חורגת מ-375px.

**וזה הבאג המתועד**, שתוקן פעם אחת בעמודי המכשיר ושם `gen-devices.js` מזריק את הכלל.
הפיגום `new-page.js` מעתיק את המסגרת מ-`phone-problems/index.html`, שאין בו כפתור הירו ארוך,
**ולכן התיקון לא נסע יחד עם המסגרת.** אותה משפחה בדיוק של `.hub` ו-`.chip` ו-`.cmp`.

הכלל `.ghero .btn-hero{white-space:normal;text-align:center}` הוחל על שני המדריכים החדשים.
אחרי התיקון, בשני העמודים ובשני הרוחבים ובכל 11 מצבי הנגישות: **אפס גלישה.** הכפתור נשבר
לשתי שורות ב-375px תחת 150% (336x111), וזה מה שהצ׳קליסט קובע שעדיף.

✅ **והפריט המבני נסגר.** הכלל נוסף ל-`prototype/phone-problems/index.html`, הקובץ ש-`new-page.js`
מעתיק ממנו את המסגרת, ולכן **כל עמוד עתידי יורש אותו**. זה גם תיקן סיכון שהיה שם בלי שאיש ידע:
הכפתור באותו עמוד מודד 348px מתוך 360 תחת הגדלה ל-150%, כלומר 12px מרווח, ותווית אחת ארוכה
יותר הייתה שוברת אותו.

הוכח: עמוד נשתל בפיגום, נמצא בו הכלל, והוסר.

---
## D8 — QA סופי

8 מסלולי §16 על כל האזור, ובנוסף השניים שאף כלי לא בודק:

1. **התאמה בין ה-Schema למה שהעמוד באמת מציג.**
2. **קריאת כל עמוד בישיבה אחת וספירת הצורות** מ-`pg-new-content-page` §7. 12 עמודי מכשיר מתבנית
   אחת הם הצורה שמייצרת פרוזה מכונית, וזה הסיכון האיכותי הגבוה ביותר באזור.

### שני כללי כתיבה שנולדו מהבנייה עצמה

שניהם נאכפים עכשיו ב-`copy-audit.js`, ושניהם התגלו רק בגלל שמדדו. **התיקון תמיד ב-`devices.json`
ולא ב-HTML**, אחרת ההרצה הבאה של המחולל מוחקת אותו.

1. **הגוף לא חוזר על מחרוזת המפרט מילה במילה.** הטבלה מציגה, הגוף מסביר מה זה אומר. שורת
   "סוללה" שכותבת שוב "עד 29 שעות הפעלת וידאו" היא חזרה ולא הסבר. נתפס בבדיקת הרצפים החוזרים.
2. **אותו נתון לא נכתב שלוש פעמים.** מבנה העמוד הוא שלושה מקטעים שכולם על אותו דגם, ולכן
   הנתון הבולט נכתב שלוש פעמים כמעט מעצמו. הבדיקה `אותו נתון חוזר שלוש פעמים ומעלה` מצאה את
   זה בשבעה עמודים, כולל ארבעה שכבר עברו ביקורת ונחשבו נקיים.

---

## ההחלטות שממתינות לאופק

| # | ההחלטה | למה עכשיו |
|---|---|---|
| 1 | **רשימת הדגמים.** 12 נבנו, והאימות פתח שש שאלות ספציפיות | ראה `_candidates_findings` ב-`devices.json` |
| ~~2~~ | ~~התיקייה~~ | ✅ **נענה בפועל:** כל הסשן עבד ומקמט מהתיקייה הראשית |
| 3 | 8 אירועים עם פרמטרים, או 25 שמות | 25 = 25 תגי GTM ידניים |
| ~~4~~ | ~~יד שנייה באזור?~~ | ✅ **נענה 5.8.2026: אין מכשירי יד שנייה למכירה.** מודל הנתונים הוא מכשירים חדשים בלבד, בלי שדה מצב מכשיר ובלי מסלול אחריות שני |
| ~~5~~ | ~~מחירון?~~ | ✅ **נענה 6.8.2026: אין מחירון באתר.** המחיר משתנה כל הזמן ולכן נמסר בשיחה. ראה למטה |
| ~~6~~ | ~~3 עמודות בהשוואה ב-375px~~ | ✅ **הוכרע במדידה 6.8.2026.** ראה D3.2 למעלה |
| 7 | מי מתחזק את עמודי השמועות | לפני D6.1 |
| 8 | ביקורות על **קנייה** | ל-D1.4 |
| 9 | **קישורי הפוטר ב-13.6px** ובהדר ב-15.2px | מסגרת משותפת, נוגע גם ל-`index.html` |

**נענו: 2, 4, 5, 6. פתוחות: 1, 3, 7, 8, 9.**

### החלטה 5, ומה נגזר ממנה

**אין מחירון באתר.** המחיר משתנה כל הזמן, ולכן הוא נמסר בטלפון או ב-WhatsApp.
`commercial.price` נשאר `null` **בכוונה ולא כחוסר**, ולכן הוא ירד מרשימת "חסר מאופק"
(14 שדות ולא 15) ובמקומו יש התרעה במחולל אם מישהו כן ימלא אותו.

זה שינה נוסח ולא רק נתון. הכיתוב מעל הטבלה אמר "שדה בהדגשה נטויה **עדיין לא עודכן**",
ולגבי המחיר זה הפסיק להיות נכון: הוא לא ממתין לעדכון, הוא לא יופיע. **עמוד שמרמז שמחיר
בדרך מטעה את מי שקורא אותו.** שורת המחיר אומרת עכשיו למה, ולא רק מה לעשות.

**המשמעות לגוגל, בקול ולא בדיעבד:** `Offer` דורש `price`, וגם `AggregateOffer` דורש
`lowPrice`. בלי מחיר, **12 עמודי המכשיר לא יהיו זכאים לתוצאות עשירות של מוצר.** הבלוק
שבונה את ה-`Offer` נשאר בקוד ולא נמחק, כדי שאם ההחלטה תשתנה הוא פשוט יעבוד.

מה שכן נשמר: 12 מילות המפתח עם המילה "מחיר" נשארות במאגר, כי אנשים מחפשים כך. אף
`meta description` ואף כותרת אינם מבטיחים מחיר, ונבדק. הכפתור הראשון בעמוד הוא
"בדיקת מחיר ומלאי ב-WhatsApp", כלומר מי שהגיע מחיפוש "מחיר" מקבל תשובה בלחיצה אחת.

---

*נכתב 5.8.2026 על בסיס `DEVICES-AREA-PLAN.md` גרסה 2. טרם נכתבה שורת קוד אחת.*
