# אודיט שישה צוותים, 6.8.2026

שישה סוכנים במקביל: הגהה עברית, נגישות, מערכת עיצוב, איכות נתונים, סקירת קוד, ואבטחה.
הקובץ הזה קיים כדי ששום ממצא לא ייעלם. **מה שתוקן מסומן, ומה שלא, לא.**

---

## תוקן

| # | ממצא | מי מצא | התיקון |
|---|---|---|---|
| 1 | **bidi הפך את סדר המספרים בכל תא מפרט מעורב.** `50MP, 12MP` הוצג `12MP 50MP`, כלומר **המצלמה הראשית נראתה 12MP**. `162.2 x 77.5 x 7.4` הוצג `162.2 7.4 x 77.5 x`. 20 עמודים | עיצוב | `lib/bidi.js` חדש, `ltrRuns` עוטף כל ריצה לטינית ב-`<bdo>`. הוחל בשני המחוללים. **122 עוטפים, אפס סדר שגוי** |
| 2 | **`devices.json` הפרטי הוגש לדפדפן במלואו**, כולל `_rules`, `_candidates_findings`, `spec_source`, ומקום לציטוטים לפני אישור | אבטחה + נתונים | `devices-public.json` נגזר במחולל, 4 שדות, 15KB מול 137KB. הכלי שולף אותו. `vercel.json` מפנה את הפרטי. חגורה שנופלת אם שדה פרטי ידלוף |
| 3 | **`/phones/` הצהיר בסכימה שהוא מדריך היבוא.** `CollectionPage` עם ה-`@id` וה-`headline` של המדריך | הגהה | הבלוק הוחלף ב-`CollectionPage` נכון של `/phones/` |
| 4 | 15 שגיאות עברית: תחביר שבור, פועל פעיל במקום סביל, התאמת מין, ותווית שסתרה את הגוף שלה | הגהה | תוקן ב-`devices.json` ובמחוללים, לא ב-HTML המחולל |
| 5 | `d.slug` נכנס ל-`href` בתוך `innerHTML` בלי `esc` | אבטחה | עטוף |

---

## לא תוקן, לפי סדר חומרה

**E3 · קו הסוללה משווה שעות ל-mAh.** ב-`galaxy-a56-vs-redmi-note-14-pro` הקו מציג **1% מול 100%**,
כלומר אומר לקורא שהסוללה של ה-A56 היא מאית מזו של הרדמי. בפועל 5000 מול 5500 mAh.
`traits.js` › `NUMERIC_BY_FIELD.battery` מחזיר שעות אם יש, ואחרת mAh, בלי לבדוק שהיחידות זהות.
**התיקון:** להחזיר `null` כששני הצדדים אינם באותה יחידה, ואז לא ייצבע קו בכלל.

**E4 · פירורי הלחם ב-`/phones/`** עוד מצביעים למדריך היבוא ב-position 3. תוקן ה-`CollectionPage`
ולא ה-`BreadcrumbList`. **התיקון:** למחוק את position 3.

**E5+E2 · השאלון מעניש ארבעה דגמי סמסונג על עדשה שיש להם.** העדשות של סמסונג יושבות בתוך
`camera_main` ולא ב-`camera_extra`, ולכן `uw` יוצא 0 והשאלון מוריד 9 נקודות בשאלת "לצלם קבוצות".
מאותה סיבה 25 תאים בעמודי ההשוואה אומרים "לא מפורסם אצל היצרן" על דברים שכן מפורסמים.
**התיקון:** לפצל את `camera_extra` בסמסונג ב-`devices.json`. זה סוגר את שני הממצאים.

**R2 · `gen-finder.js` אינו עושה `require` ל-`traits.js`** ומממש מחדש ארבעה פרסרים. זה בדיוק
מה ש-`traits.js` נוצר למנוע, ו-E5 הוא מה שהצורה הזאת מייצרת: שני העותקים מסכימים היום, ולכן
אף בדיקה לא תופסת, והתיקון צריך לקרות פעמיים.

**G2 · אף אחת מ-6 הבדיקות של D0.8 לא קיימת ב-`preflight.js`.** אין בדיקה שאין `Offer`, אין
בדיקת מקור לכל שדה, אין בדיקת סנכרון `devices.json` ↔ HTML. **זה השורש של G1 ושל E2:** הכללים
כתובים ואינם נאכפים. לוח המצב אומר "D0 הושלם" בזמן שרק D0.1 ו-D0.2 עברו.

**E6+R4 · ניסוחים שיוצרים "הבדל" שאינו קיים.** הכיתוב בראש עמוד ההשוואה אומר "זהים" והטבלה
מתחתיו מציגה הבדל, ב-8 עמודים. הסיבה: סיומות קישוט שהנרמול לא תופס (`(159.3 מ״מ)` בסמסונג
ולא באפל, `394 ppi` ברדמי הבסיסי ולא בפרו, `4K` מול `UHD`, `טיפוסי` מול `טיפוסית`).
**התיקון:** איחוד ב-`devices.json`, ואז הרצה מחדש. שווה גם בדיקה שסופרת כמה תבניות ניסוח יש
לאותו שדה בתוך מותג.

**G3 · שני המדריכים החדשים אינם ב-`services.json` ואינם ב-`/guides/`.** מרכז המדריכים אומר
"שניים כרגע" ומונה 2, בזמן שקיימים 4. הם מקשרים למרכז והמרכז לא מקשר אליהם.

**G1 · 38 שדות ריקים בלי `note`**, בניגוד לכלל ב-`_rules`. ובנוסף `undefined` ו-`null` משמשים
לאותה משמעות: אפל משמיטה מפתח, סמסונג מציבה `null`, ושדה חסר לא יכול לשאת `note`.

**R1 · `null` נכתב כעובדה שלילית.** מדריך ה-eSIM אומר בפרוזה "שני דגמי רדמי נוט 14 אינם תומכים"
בזמן שהטבלה **באותו עמוד** אומרת נכון "היצרן אינו מציין תמיכה". במאגר זה `null` בלי `note`.

**R5 · A56 מול A36: שני התאריכים במרווח שנה, ושתי המחרוזות אומרות "שש שנים".** לא הוכח שגוי,
וצריך לקרוא מחדש את שני עמודי המפרט.

**R3 · `chip` מחזיק שני דברים לפי מותג.** אפל שם שבב, סמסונג ליבות ושעונים. בהשוואה השורה
"מעבד" משווה שם לרשימת שעונים, ובסמסונג היא משכפלת את השורה "ליבות CPU" שמתחתיה.

**נגישות והיסט הסרגל הדביק:** `.qcount` ו-`.dstate` יושבים ב-`inset-block-start:66px`, אבל
מתחת ל-980px ההדר אינו דביק. **התיקון:** באותה מדיה־קוורי, `inset-block-start:0`.

**כפתור ההירו** חסר את `white-space:normal` ב-`/phones/`, `/guides/`, ובמדריך היבוא. לא גולש
היום, מלכודת לשינוי קופי.

**`.req` היא מחלקה מתה** בשאלון. הסימון "(נדרש)" מקבל אותו גודל וצבע כמו ה-`legend`.

**R6 · "קל ב-16 גרם"** — ההפרש 16.5.  **R7 · "1 שדות זהים"** — צריך "שדה אחד זהה".

**קיים מראש, לא מהדיף הזה:** לאתר אין Content-Security-Policy בכלל. ו"קרית גת" מול "קריית גת"
מעורב בין משפחות עמודים, וזו הכרעת סגנון של אופק.

---

## מה שנבדק ונמצא נקי

מבחן הקבלה של מערכת העיצוב עבר ב-**28 העמודים**: `0px` דומיננטי, `4px` לכפתורים בלבד, אפס
גלולות, אפס `box-shadow` על תוכן, `h1` סריף בכל אחד, אפס מאפיינים פיזיים `left`/`right`.

**מחירים וסכימה נקיים לגמרי:** אפס `Offer`, אפס `price`, ב-121 בלוקי JSON-LD. `commercial` הוא
`null` ב-144 מתוך 144 שדות, ו-`recommendation` הוא `missing` ×24.

**כיסוי:** 12 דגמים ↔ 12 עמודים, 8 זוגות ↔ 8 עמודים, וכל 41 הכתובות ב-`sw.js` קיימות בפועל.

**נאמנות למקורות:** מדריך היבוא ומדריך האחסון תואמים לקבצי המקורות שלהם עד הספרה האחרונה,
ומסרבים במפורש לכל פריט שלא אומת.

**אבטחה:** XSS נקי בשני הכלים, קישור ה-WhatsApp מקודד והיעד קבוע, `PG_PROD` מגודר נכון,
ה-service worker אינו שומר POST ואינו שומר חוצה־מקור, ואין תלות npm חדשה.
