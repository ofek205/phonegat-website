# עמודי שירות: תוכנית עבודה לפני פיתוח

תשובה לסעיף "פלט נדרש מקלוד לפני פיתוח" באפיון גרסה 1.1 (31 ביולי 2026).
נכתב מול `origin/main` בקומיט 5c6018e. **טרם בוצע שינוי קוד כלשהו.**

---

## 1. הטכנולוגיה ומבנה האתר שנמצאו

| נושא | מה נמצא |
|---|---|
| ארכיטקטורה | ארבעה קבצי HTML שטוחים ב-`prototype/`. **אין שלב build, אין `package.json`, אין תלויות npm** (החלטה מכוונת ב-CLAUDE.md) |
| Routing | אין. Vercel מגיש קבצים סטטיים, שורש הפריסה `prototype/`. `/phone-problems.html` הוא הקובץ עצמו |
| Head ו-Schema | לכל דף `<head>` משלו עם `title`, `description`, canonical, og, twitter, ובלוקי JSON-LD נפרדים. ישות `#business` מוגדרת במלואה בכל דף, לא רק מוזכרת |
| קומפוננטות | אין. **כל דף נושא עותק מלא של המסגרת המשותפת** (הדר, ניווט, פוטר, תפריט נגישות, באנר קוקיז, PWA, סרגל מובייל) |
| CSS | inline בכל דף. אסימונים זהים: `--teal:#1878A8`, מבטאים סגול/ירוק/כתום, `--font` היא Assistant/Heebo. הסריף העריכותי שמור ל-`main` בלבד |
| סביבות | מקומי / `staging` (`*.vercel.app`) / `main` (פרודקשן). `ship.js stage` ואז `prod` |
| שער איכות | `.githooks/pre-push` חוסם דחיפה ל-`main` אלא אם `preflight.js` עובר **וגם** הקומיט המדויק כבר ב-`origin/staging` |
| בדיקות | `preflight.js`, 20 בדיקות סטטיות |

### שלוש דרישות באפיון שכבר מיושמות במלואן

- **`noindex` ב-Staging** — קיים בשתי שכבות: `X-Robots-Tag` ב-`vercel.json` לכל `*.vercel.app`, ומטא בדף.
- **פרודקשן ללא `noindex` בטעות** — `preflight` בודק את זה.
- **סביבת Preview** — קיימת, כולל באנר "סביבת בדיקות" ושומר `PG_PROD` שמשתיק GA4, שליחת טופס ומונה הקופונים מחוץ לדומיין האמיתי.

---

## 2. מפת URL: שלוש אפשרויות, והמלצה

האפיון מבקש `/iphone-repair-kiryat-gat/` וגם מורה לאשר את המבנה מול הטכנולוגיה הקיימת. **הבדיקה מצאה שלושה חסמים שהופכים את זה להחלטה אמיתית ולא לפורמליות.**

### מה נמצא

1. **כל נתיבי הנכסים באתר הם יחסיים.** `src="whatsapp-logo.png"`, `href="index.html"`, `src="problems/battery.jpg"`, `register('sw.js')`. דף שיושב ב-`/iphone-repair-kiryat-gat/index.html` יפתור כל אחד מהם לתיקייה שלו ויקבל 404.
2. **ה-service worker חמור יותר מהשאר.** `register('sw.js')` מתוך תיקייה ירשום עובד עם scope של אותה תיקייה בלבד, כלומר PWA שבור ולא רק תמונה חסרה.
3. **`preflight.js` סורק `readdirSync` לא רקורסיבי על `prototype/`.** עמוד בתוך תיקייה **לא ייסרק על ידי אף אחת מ-20 הבדיקות.** זו בדיוק התקלה שהמדריך יצר ושבגללה נבנתה מערכת הבדיקות.

### האפשרויות

| | מבנה | הכתובת בפועל | עלות | סיכון |
|---|---|---|---|---|
| **א** | `prototype/iphone-repair-kiryat-gat/index.html` | `/iphone-repair-kiryat-gat/` | המרת כל הנתיבים היחסיים לשורשיים בפיגום, והרחבת `preflight` לסריקת עומק | בינוני, וכולו חד־פעמי |
| **ב** | `prototype/iphone-repair-kiryat-gat.html` | `/iphone-repair-kiryat-gat.html` | אפס | אפס. לא תואם את הכתובת שבאפיון |
| **ג** | `cleanUrls: true` ב-`vercel.json` | `/iphone-repair-kiryat-gat` | נמוך | **גבוה: משנה גם את כתובות הדפים הקיימים.** Vercel יפנה 308 מ-`/phone-problems.html`, בניגוד ל"אין לשנות כתובות קיימות" |

### ההמלצה: אפשרות א

הכתובת היא הדבר הכי יקר לשנות אחרי פרסום (301, איבוד סיגנל, עדכון sitemap וקישורים), והאפיון נקב בה במפורש. שלוש העלויות שלה מכניות, נעשות פעם אחת בסקריפט הפיגום, ומשרתות גם את כל העמודים העתידיים. הדפים הקיימים לא זזים.

**אפשרות ג נפסלת** כי היא נוגעת בכתובות קיימות בלי שנתבקשה, וזה אוסר מפורשות באפיון ובתקציר.

### מפת ה-URL הסופית (לפי אפשרות א)

| עמוד | URL | קובץ |
|---|---|---|
| מעבדת סלולר (מרכז) | `/mobile-phone-repair-kiryat-gat/` | `prototype/mobile-phone-repair-kiryat-gat/index.html` |
| תיקון אייפון (פיילוט) | `/iphone-repair-kiryat-gat/` | `prototype/iphone-repair-kiryat-gat/index.html` |
| החלפת מסך | `/phone-screen-replacement-kiryat-gat/` | `prototype/phone-screen-replacement-kiryat-gat/index.html` |
| החלפת סוללה | `/phone-battery-replacement-kiryat-gat/` | `prototype/phone-battery-replacement-kiryat-gat/index.html` |
| שקע טעינה | `/charging-port-repair-kiryat-gat/` | `prototype/charging-port-repair-kiryat-gat/index.html` |

תמונות: `prototype/img/<slug>/`, כדי שהן יישארו בנתיב שורשי אחד ולא ייכפלו בכל תיקיית עמוד.

**אין התנגשות** עם כתובת קיימת או עם עוגן בדף הבית. העוגנים הקיימים הם `#team`, `#reviews`, `#services`, `#deals`, `#press`, `#map`, `#faq`, `#contact`, וכולם בתוך `index.html`.

---

## 3. קבצים שיתווספו וישתנו

### נוספים

- חמישה `prototype/<slug>/index.html`.
- `prototype/img/<slug>/` עם התמונות של כל עמוד.

### משתנים (כל אחד דורש הצגה ואישור לפני ביצוע)

| קובץ | השינוי | היקף |
|---|---|---|
| `prototype/sitemap.xml` | חמש כתובות חדשות | תוספת בלבד, אחרי אישור לפרודקשן |
| `prototype/sw.js` | חמש כתובות ל-`SHELL`, והעלאת `CACHE` ל-`pg-v3` | תוספת בלבד |
| `prototype/index.html` | קישור מאריח "מעבדת תיקונים" לעמוד המרכז, ופריט ניווט אחד | כירורגי |
| `prototype/phone-problems.html` | קישור מ-`#charging`, מ-`#battery` ומ-`#no-power`, ופריט ניווט אחד | כירורגי |
| `prototype/privacy.html`, `prototype/accessibility.html` | פריט ניווט אחד, ותאריכי ביקורת הנגישות | מינימלי |
| `.claude/skills/pg-new-content-page/scripts/new-page.js` | פיגום לתיקייה, נתיבים שורשיים | תשתית |
| `.claude/preflight.js` | סריקת עומק, ובדיקות ייעודיות לעמודי שירות | תשתית |

### למה הניווט נוגע בארבעה דפים

`preflight` אוכף שהניווט זהה בכל הדפים, ולכן כל תוספת חייבת להיכנס לכולם. הניווט כבר מחזיק תשעה פריטים.
**ההמלצה: פריט אחד בלבד, "מעבדת סלולר", שמצביע לעמוד המרכז**, והוא זה שמקשר לארבעת הממוקדים. זה גם מה שהאפיון מתאר: עמוד מרכז שמקשר לעמודים הממוקדים במקום להעתיק מהם.

---

## 4. רכיבים: מה ממוחזר ומה ייחודי

`phone-problems.html` הוא בפועל ספריית הרכיבים. ההתאמה לשמות שבאפיון:

| רכיב באפיון | מה קיים | פער |
|---|---|---|
| ServiceHero | `.ghero` עם `h1`, `.sub`, `.hcta`, `.meta` | להוסיף שלושה יתרונות וכתובת ללא גלילה |
| SymptomCards | `.prob` (שורות מתחלפות), `.toc` | להמיר לרשת כרטיסים |
| ProcessSteps | `.steps` | מוכן |
| TrustSection | `.rules`, `.aside`, ביקורות מדף הבית | מוכן |
| FAQAccordion | `details/summary` בדף הבית, כולל תמיכת מקלדת | **להעתיק לדפי השירות. לא קיים במדריך** |
| ContactCTA | `.cta`, `.btn-wa`, `.mbar` | מוכן |
| Breadcrumbs | סכמת `BreadcrumbList` בלבד | **פירורי לחם חזותיים לא קיימים באף דף. רכיב חדש** |
| BeforeAfter | לא קיים | **רכיב חדש. תלוי בצילומים** |
| QuickDiagnosis | `.picker`/`.chip` (בורר המכשיר) קרוב מאוד | להרחיב לשלושה שלבים ולפלט WhatsApp |

**מה שנבנה מאפס לכל עמוד: האלמנט הייחודי בלבד.**

| עמוד | האלמנט הייחודי |
|---|---|
| מעבדה | מפת שירות מקומית עם כפתור ניווט. ללא זמן נסיעה קבוע |
| אייפון | בדיקת התאמה מהירה: דגם, תקלה, האם נדלק, ובסוף WhatsApp עם הודעה מוכנה |
| מסך | סליידר Before/After של אותו מכשיר |
| סוללה | מדריך בדיקת בריאות סוללה, ובדיקת סימפטומים |
| שקע טעינה | מקרו לפני ואחרי ניקוי, וטבלת סימפטומים |

### מבנה נתונים להרחבה עתידית

האפיון מבקש מבנה שמאפשר הוספת שירותים בלי שכפול. בלי שלב build אין תבניות, ולכן ההצעה היא **מניפסט אחד**, `prototype/services.json`, שמחזיק לכל שירות slug, שם, תקציר ותקלות מקושרות. כרטיסי השירות בעמוד המרכז ובקישורים ההדדיים נבנים ממנו, וממנו גם ייגזרו הבדיקות ב-`preflight`. הוספת שירות שישי היא רשומה במניפסט ועמוד אחד, ולא עריכה ידנית בחמישה מקומות.

---

## 5. מה חסר לביצוע

### מידע עסקי (האפיון מגדיר את בעלי העסק כמקור האמת)

| # | מה | למה חוסם | ברירת מחדל אם לא יגיע |
|---|---|---|---|
| 1 | **חלק מקורי מול תחליפי, והניסוח המאושר** | זו שאלה 2 ב-FAQ של האייפון ושאלה 7 במסך. אין עליה מילה באתר | Placeholder גלוי |
| 2 | **משך האחריות לפי סוג תיקון** | האתר אומר "משתנה לפי סוג התיקון" בלי מספרים | הניסוח הקיים, ללא מספרים |
| 3 | **האם ניקוי שקע בסיסי עדיין ללא עלות** | זה המסר המבדל של עמוד שקע הטעינה. האפיון מבקש לאמת שהמדיניות עדיין תקפה | לא נכתב עד אימות |
| 4 | **זמני טיפול שניתן לפרסם באחריות** | האתר אומר "לרוב באותו יום" | הניסוח הקיים |
| 5 | **טווחי מחיר, אם בכלל** | האפיון אוסר מחיר אוטומטי ללא מקור מחירים אמין | בלי מחירים. הפניה להצעת מחיר |
| 6 | **דגמי iPhone שמטופלים בפועל** | האפיון מבקש 17 עד 13 ומוקדם יותר לפי זמינות חלקים | Placeholder |
| 7 | **קישור מדויק לפרופיל Google** | לביקורות ולניווט | הקיים באתר |
| 8 | **המספר שמחובר ל-WhatsApp** | באתר מופיעים שניים: 052-5893366 ו-08-6812050, וקישורי `wa.me` מצביעים ל-`97286812050` | הקיים בקוד |
| 9 | **מדיניות גיבוי, קוד גישה ופרטיות בתיקון** | האפיון מקצה לזה מקטע שלם בעמוד האייפון | Placeholder |
| 10 | **ביקורות אמיתיות מתאימות לכל שירות** | האפיון דורש ביקורת רלוונטית לשירות, לא ביקורת כללית | הביקורות הקיימות בדף הבית |

### צילומים

**אין באתר אף צילום מהמעבדה, מהחנות או מתיקון.** מה שקיים הוא צילומי צוות ותדמית, ותמונות המדריך שהן אילוסטרציות. האפיון דורש לפחות צילום מקורי אחד בכל עמוד לפני אישור סופי, ורשימת צילומים בת 15 עד 20 פריטים.

הפיילוט ייבנה עם `.fig .ph`, מסגרת פלייסהולדר שקיימת בקוד בדיוק למטרה הזאת, עם תיאור הצילום הנדרש ו-`alt` כתוב מראש. החלפה בצילום אמיתי היא פעולה מכנית.

**שני צילומים חוסמים את הפיילוט עצמו:** אייפון על שולחן המעבדה במהלך בדיקה (תמונת ה-Hero), ואיש צוות בודק מכשיר.

---

## 6. סיכונים ומגבלות

| # | סיכון | חומרה | טיפול |
|---|---|---|---|
| 1 | **עמוד בתיקייה בורח מ-20 הבדיקות** | גבוהה | להרחיב את `preflight` לסריקת עומק **לפני** יצירת העמוד הראשון, לא אחריו |
| 2 | **נתיבים יחסיים שבורים בעומק** | גבוהה | המרה לשורשיים בפיגום, ובדיקת `preflight` שתיכשל על נתיב יחסי בעמוד עומק |
| 3 | **scope של ה-service worker** | בינונית | `register('/sw.js', {scope:'/'})`, ואימות בדפדפן |
| 4 | **קניבליזציה בין חמישה עמודים באותה עיר** | בינונית | האפיון כבר מגדיר לכל עמוד ביטוי, כוונה ורשימת "אין להתחרות על". להוסיף בדיקה ש-`h1` ו-`title` ייחודיים בין העמודים |
| 5 | **"קרית גת" מול "קריית גת"** | בינונית | ראו למטה |
| 6 | **`index.html` בן 343KB משותף עם סשן מקביל** | בינונית | `git fetch` לפני עריכה, commit של הקבצים שלי בלבד, בלי `git add -A` |
| 7 | **אירועי GA4 חדשים לא נמדדים בלי תג ב-GTM** | בינונית | הקוד דוחף ל-`dataLayer`, אבל התג נוצר בממשק GTM שאינו ברפו. תלות בבעל האתר |
| 8 | **סליידר Before/After בלי צילומים** | נמוכה | לא נבנה עד שיש זוגות אמיתיים של אותו מכשיר |
| 9 | **סתירת יחסי תמונות** | נמוכה | האפיון: Hero דסקטופ 16:9 או 3:2, מובייל 4:5 או 3:4. הסקיל: 3:4 בתוכן עם חיתוך 4:3 במובייל. אלה אלמנטים שונים ולא סתירה. יתועד כדי שלא "יתוקן" |

### הסתירה בכתיב "קריית גת"

האפיון מורה על "קריית גת" בעקביות, ואוסר עמודים נפרדים לשתי הצורות. **האתר הקיים כותב "קרית גת" בכל מקום**, כולל בסכמת `#business` ובכתובת העסק.

ההמלצה: העמודים החדשים משתמשים ב"קריית גת" ככתיב הראשי, ומשלבים "קרית גת" באופן טבעי בגוף הטקסט, בדיוק כפי שהאפיון מתיר. **הדפים הקיימים לא נוגעים.** שינוי הכתיב בכל האתר הוא פרויקט נפרד שנוגע בשם העסק ובנתונים מובנים, והוא לא בהיקף הזה.

---

## 7. תוכנית בדיקות ו-Rollback

### לפני כל עלייה ל-staging

1. `node .claude/preflight.js` (20 בדיקות, ובנוסף מה שיתווסף לעמודי עומק).
2. מדידה בדפדפן, לא התרשמות: אפס גלילה אופקית ב-375px, כל פקד מעל 44px, ניגודיות מחושבת מול הרקע האפקטיבי.
3. כל קישור WhatsApp, חיוג וניווט נבדק בפועל.
4. אפס שגיאות Console חדשות.

### מגבלות סביבה שאסור לפרש כבאגים

חלונית הדפדפן כאן מדווחת `visibilityState: hidden`, ולכן **IntersectionObserver לא מספק callbacks כלל**. כל מדידת גלילה או "נקרא" תיבדק בהפעלה ישירה של המטפל, ובמכשיר אמיתי. צילומי מסך במובייל אינם אמינים כאן. **מה שלא אומת ייאמר במפורש כלא אומת.**

### Rollback

הפריסה מבוססת git, ולכן החזרה היא `git revert` ודחיפה.

- **העמודים החדשים אדיטיביים.** מחיקתם לא נוגעת בשום דף קיים.
- **העריכות בדפים הקיימים הן קישורים ופריט ניווט בלבד**, כולן בקומיטים נפרדים מהעמודים עצמם, כדי שאפשר יהיה להחזיר את החיבורים בלי להחזיר את העמודים.
- **אין מחיקה ואין שינוי של כתובת קיימת** בשום שלב.
- אם עמוד כבר אונדקס ואז יוסר, תידרש הפניית 301 לעמוד המרכז. לכן הכתובות מאושרות לפני הפרסום ולא אחריו.

---

## הצעד הבא

שלוש החלטות חוסמות: מבנה ה-URL, פריט הניווט, וכתיב "קריית גת".
אחריהן: הרחבת `preflight` והפיגום, ואז wireframe של עמוד האייפון לאישור, ורק אחריו כתיבת העמוד.
