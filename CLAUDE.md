# PHONE GAT — הנחיות פרויקט

אתר פון גת (מעבדת וחנות סלולר, קרית גת). עברית, RTL.

## מבנה
- `prototype/index.html` — **כל האתר בקובץ אחד** (HTML + CSS + JS inline). זו החלטה מכוונת, לא חוב טכני.
- `prototype/api/` — פונקציות סרברלס של Vercel.
- `prototype/` היא ה-**Root Directory** בפריסה, ולכן `prototype/api/coupon.js` נגיש כ-`/api/coupon`.
- **אין `package.json` בפרויקט, וזה בכוונה.** אל תוסיף תלויות npm. אם צריך שירות חיצוני — קרא ל-REST API שלו ב-`fetch`.

---

## סביבות ותהליך העלאה

| סביבה | כתובת | ממה נוצרת |
|---|---|---|
| מקומית | `http://localhost:8000` | `.claude/launch.json` (שרת סטטי, ללא תלויות) |
| בדיקות | `…-git-staging-….vercel.app` | דחיפה לענף `staging` |
| פרודקשן | `www.phonegat.co.il` | דחיפה ל-`main` |

**התהליך:** עבוד על `staging` → דחוף → בדוק בכתובת ה-vercel.app (גם בטלפון אמיתי) → מזג ל-`main`.
כל ענף מקבל כתובת משלו ב-Vercel, כך שאפשר גם ענף נפרד לכל שינוי.

**לפני כל דחיפה ל-`main`:**
```
node .claude/preflight.js
```
יוצא בקוד 1 ומסביר מה נשבר. תופס בין היתר את מלכודת רשימת ההיתר של הקופונים (למטה), אי-התאמה בין שאלות ה-FAQ לסכמה, ו-JSON-LD שבור.

### `PG_PROD` — ההגנה שאסור להסיר
`window.PG_ENV` / `window.PG_PROD` מוגדרים בראש ה-`<head>`. **פרודקשן = הדומיין האמיתי בלבד**; `localhost` וכל `*.vercel.app` הם סביבת בדיקות. ארבעה דברים מושתקים שם, כי כל אחד מהם מזהם נתון שאי אפשר להחזיר:

- **GTM/GA4 לא נטען** — בדיקה לא תיספר כלקוח אמיתי. `dataLayer` ו-`track()` ממשיכים לעבוד, כך שחיווט האירועים נשאר בדיק. `?pg_gtm=1` מכריח טעינה כשצריך לבדוק תג.
- **הטופס והליד מהצ'אט לא נשלחים** — פנייה מבדיקה לא תגיע לתיבה של ברוך וסיגל. הוולידציה כן רצה במלואה, והמשתמש מקבל הודעה שזו בדיקה.
- **מונה הקופונים לא נגרע** — `fetchSeq` מחזיר `null` ונופל לטווח `9xxx`, כי ערך המונה **הוא** התשובה ל"כמה קופונים הונפקו".
- **`noindex`** — גם ב-`vercel.json` (כלל `X-Robots-Tag` לכל `*.vercel.app`) וגם כ-meta, כדי שגוגל לא יאנדקס עותק בדיקות במקום האתר.

בנוסף מוצג באנר אדום "סביבת בדיקות" כדי שלא יבלבלו בין staging לאתר החי.

**כשמוסיפים אינטגרציה חדשה שכותבת לעולם האמיתי** (מייל, מונה, תשלום, CRM) — עטוף אותה ב-`if(!window.PG_PROD)` והוסף בדיקה ל-`preflight.js`.

### עריכה מקבילה
לפעמים שני chats עורכים את `index.html` יחד. לפני עריכה: `git pull` ובדיקת `git status`. לפני commit — ודא שאתה מעלה רק את השינויים שלך (אפשר לבודד עם patch חלקי), כדי לא לדחוף עבודה לא-גמורה של הסשן האחר.

---

## קודי קופון — חובה לכל מבצע חדש

לכל מבצע עם קוד קופון **חייב** להיות קוד בפורמט `XX-NNNN`: שתי אותיות שמזהות את **סוג** המבצע (לא את המחיר), ואחריהן מספר סידורי רצוף מהמונה בשרת. המספר האחרון שהונפק הוא התשובה לשאלה "כמה קופונים נוצרו".

**זו התנהגות ברירת המחדל — אין צורך לבקש אותה.** כל מבצע חדש שנוסיף יעבוד כך.

### שלושה מקומות לעדכן, תמיד ביחד

1. **`prototype/index.html`** — הוספת רשומה ל-`OFFERS` בבקר הקופונים:
   ```js
   tradein:{title:'…',desc:'…',price:'…', … ,key:'pg_tradein_coupon',prefix:'TR-', … }
   ```
   `prefix` — שתי אותיות שעוד לא בשימוש. `key` — מפתח `localStorage` ייחודי.

2. **`prototype/api/coupon.js`** — הוספת מזהה המבצע לרשימת ההיתר:
   ```js
   var OFFERS = { screen: 1, kb: 1, tradein: 1 };
   ```

3. **הטריגר ב-HTML** — `<button data-pg-coupon="tradein">`. ערך ריק (`data-pg-coupon`) = מבצע מגן המסך.

### ⚠️ המלכודת
אם תוסיף מבצע ל-`OFFERS` בלקוח **ותשכח את רשימת ההיתר בשרת**, השרת יחזיר 400, והלקוח ייפול בשקט לקוד בטווח `9xxx`. האתר ימשיך לעבוד, הלקוחות יקבלו קודים תקינים — **אבל המבצע לא ייספר בכלל**. אין שגיאה גלויה למשתמש. תמיד עדכן את שני הצדדים.

### עקרונות שאסור לשבור
- **`POST` בלבד** לנקודת הקצה. תשובת `GET` עלולה להיות מוקצדת ב-CDN, ואז שני לקוחות יקבלו את אותו מספר.
- **הפופ-אפ לא מחכה לרשת.** נפתח מיד עם פלייסהולדר `••••`, וכפתורי ההעתקה וה-WhatsApp חסומים עד שהקוד מגיע — כדי שלא יישלח פלייסהולדר ללקוח.
- **קוד ששמור ב-`localStorage` מוצג סינכרונית** ולא צורך מספר חדש. לקוח חוזר רואה את אותו קוד.
- **הטוקן של המונה נשאר בצד שרת בלבד.** אסור להזכיר אותו ב-HTML/JS של הדף.
- **טווח `9xxx` שמור לנפילות.** אל תנפיק ממנו מספרים רגילים.

### קריאת המדד
`pg:cpn:seq:<offer>` ב-Upstash. הערך של המפתח = כמה קופונים הונפקו למבצע הזה.

---

## כתיבה ותוכן
- **אל תשנה טקסט של ביקורות לקוחות.** הן אמיתיות. גם כשמתקנים עובדות באתר — לא נוגעים בציטוטים.
- "WhatsApp" באנגלית, לא "וואטסאפ".
- מספרי טלפון בתצוגה: `052-5893366` / `08-6812050`. בקישורי `tel:`/`wa.me` — ספרות בלבד.
- לא להשתמש ב-"חינם" (מזלזל במותג) — במקום זה "ללא עלות".
