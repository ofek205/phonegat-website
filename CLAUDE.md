# PHONE GAT — הנחיות פרויקט

אתר פון גת (מעבדת וחנות סלולר, קרית גת). עברית, RTL.

## מבנה
- `prototype/index.html` — **כל האתר בקובץ אחד** (HTML + CSS + JS inline). זו החלטה מכוונת, לא חוב טכני.
- `prototype/api/` — פונקציות סרברלס של Vercel.
- `prototype/` היא ה-**Root Directory** בפריסה, ולכן `prototype/api/coupon.js` נגיש כ-`/api/coupon`.
- **אין `package.json` בפרויקט, וזה בכוונה.** אל תוסיף תלויות npm. אם צריך שירות חיצוני — קרא ל-REST API שלו ב-`fetch`.

---

## סביבות ותהליך העלאה

| סביבה | כתובת | ממה נוצרת |
|---|---|---|
| מקומית | `http://localhost:8000` | `.claude/launch.json` (שרת סטטי, ללא תלויות) |
| בדיקות | `…-git-staging-<שם-הסשן>-….vercel.app` | דחיפה לענף `staging-<שם-הסשן>` |
| פרודקשן | `www.phonegat.co.il` | דחיפה ל-`main` |

### שני צעדים, תמיד באותו סדר

```
git commit -am "..."            # 1. commit מקומי
node .claude/ship.js stage      # 2. בדיקות → סביבת הבדיקות (מדפיס את הכתובת)
                                # 3. פותחים בדפדפן ובטלפון ומסתכלים
node .claude/ship.js prod       # 4. רק אחרי שראית שזה עובד → האתר החי
```

### שני הצעדים האלה דורשים אישור מפורש, בכל פעם

**אף דחיפה לא קורית בלי שאופק אמר לדחוף את השינוי הזה — גם לא לטסטים.** מסיימים את העבודה, מריצים `preflight.js`, בודקים בדפדפן מקומית, מקמטים, ואז **עוצרים ומדווחים**. הפקודה `ship.js stage` מוצעת לו, לא מורצת.

זה נקבע ב-2.8.2026. הסביבה שעליה מסתכלים היא שלו, ההחלטה שזה תקין היא שלו, ודחיפה בלי אישור לוקחת את ההחלטה במקומו. **האישור הוא לשינוי ספציפי ולא נמשך קדימה** — "אפשר לעלות" חל על מה שהוא ראה, לא על התיקון שבא אחריו.

`ship.js` דוחף את ה-HEAD המקומי ישירות לענף המרוחק ו**לא עושה `checkout`** — בכוונה. לפעמים כמה chats חולקים את התיקייה, והחלפת ענף אצל אחד מושכת את הקרקע מתחת לשני. לכן זה עובד גם מכל ענף וגם מ-worktree נפרד.

### תיקייה לכל סשן — הכלל הראשון

**כל chat עובד בתיקייה משלו, לא בתיקייה משותפת.** זה לא סדר וניקיון, זה מה שמונע אובדן עבודה.

**התיקייה היא לפי תחום עבודה, לא לפי מי שיושב בה.** כך סשן חדש יודע מיד לאן ללכת, ושם ענף הבדיקות מסביר את עצמו.

| תחום | תיקייה | ענף עבודה | ענף בדיקות |
|---|---|---|---|
| תיקונים כלליים וקופי | `C:\pg-fixes` | `work/fixes` | `staging-fixes` |
| אנימציות ועיצוב | `C:\pg-design` | `work/design` | `staging-design` |
| דפים חדשים | `C:\pg-pages` | `work/pages` | `staging-pages` |
| חיבורים חיצוניים (Vercel, GitHub, אנליטיקס, API) | `C:\pg-integrations` | `work/integrations` | `staging-integrations` |

התיקייה הראשית ב-OneDrive **אינה מקום עבודה יותר.** היא נשארת כי היא מחזיקה את `.git` ואת ההיסטוריה.

**⚠️ פותחים את הנתיב הקצר, `C:\pg-<תחום>`, ולא את תיקיית הניהול של git.** לכל worktree יש תיקייה תאומה תחת `…\phonegat website\.git\worktrees\<תחום>`, והיא נראית כמו נתיב לגיטימי אבל מכילה רק `HEAD`, `index` ו-`refs`. אין שם `prototype/` ואין `.claude/ship.js`, כלומר סשן שנפתח שם לא יכול לעבוד בכלל. זה קרה ב-3.8.2026 לשלושה סשנים במקביל.

הסימן: `git status` נכשל ב-`this operation must be run in a work tree`, ו-`ship.js` לא נמצא. הפתרון: `git worktree list` מראה את הנתיבים האמיתיים, וסוגרים ופותחים מחדש על הקצר.

**למה worktree ולא clone נפרד לכל תחום:** ההגדרה `core.hooksPath = .githooks` יושבת ב-`.git/config` המשותף, ולכן כל worktree יורש את שער הפרודקשן אוטומטית. ב-clone נפרד היה צריך להגדיר אותה ידנית בכל תיקייה, ואם שוכחים השער נעלם בשקט ואפשר לדחוף ל-`main` בלי שהטסטים עברו. worktree גם מונע מ-git להוציא את אותו ענף בשתי תיקיות, ו-clone לא.

**סשן אחד לכל תחום.** אם שני chats עובדים על עיצוב הם יתנגשו שוב, כי הם יחלקו תיקייה. אז או שמאחדים אותם, או שפותחים תחום נוסף לפי אותו מתכון:

```
git worktree add -b work/<תחום> C:/pg-<תחום> origin/main
echo <תחום> > C:/pg-<תחום>/.claude/session-name
```

ואז פותחים את ה-chat **בתיקייה הזאת**. סשן נקשר לתיקייה שלו ברגע שנפתח, ואין פקודה שמעבירה סשן רץ.

**למה תיקייה משותפת שוברת גם עבודה זהירה:** ב-2.8.2026 בדקתי `git diff`, הרצתי `git add` על ארבעה קבצים מפורשים, וקימטתי בלי `-a`. הקומיט יצא עם 15 קבצים, כי הסשן השני הריץ `git add -A` בין ה-`add` שלי ל-`commit` שלי, ואני קימטתי את כל האינדקס שלו. בפעם אחרת הקומיט שלי מחק את מקטע טופס הקשר מדף הבית באמצע עבודת המעבר שלהם ל-`/contact/`. **אין תחליף להפרדת התיקיות.**

**וגם `.claude/session-name` הוא לפי תיקייה ולא לפי סשן** — שני chats באותה תיקייה יורשים את אותו שם, מקבלים את אותו `staging-<שם>`, וענף התצוגה נהיה מעורב. זה קרה ל-`staging-general`, ולכן `ship.js` חוסם את זה היום (ראו למטה).

### ענף בדיקות לכל סשן

**כל סשן דוחף ל-`staging-<שם>` משלו, לא לענף משותף.** בפרויקט הזה נפתחים במקביל ארבעה chats שחולקים תיקייה אחת. כשכולם דחפו ל-`staging` יחיד הם דרכו זה על זה, ובגרסה שעוד הריצה `-f` זו הייתה מחיקה שקטה של עבודת האחרים — קרה ארבע פעמים ב-31.7.2026.

```
node .claude/ship.js whoami            # לאן הסשן הזה שולח
node .claude/ship.js stage animations  # קובע שם, פעם אחת
node .claude/ship.js stage             # מכאן והלאה בלי שם
```

השם נלקח לפי הסדר: `PG_SESSION`, הקובץ `.claude/session-name`, ואז שם הענף הנוכחי. הוא נשמר ב-`.claude/session-name`, ש**מתעלם בגיט בכוונה** — אילו היה בגיט, כל הסשנים היו מקבלים אותו שם וחוזרים להתנגש.

**מקף ולא לוכסן** (`staging-animations`, לא `staging/animations`): ref הוא נתיב בקובץ, ולכן `staging` ו-`staging/animations` לא יכולים להתקיים יחד, והענף הישן היה חוסם כל ענף סשן חדש. המקף גם נותן בדיוק את הכתובת ש-Vercel מייצר.

**מקסימום 16 תווים.** התווית של Vercel מוגבלת ל-63 והמעטפת תופסת 47. שם ארוך יותר נדחה ולא נחתך בשקט, כי שני סשנים שנחתכים לאותו slug חוזרים בדיוק לבאג שהמנגנון בא למנוע.

**איפה נפגשות העבודות:** ב-`main`. הדחיפה אליו היא fast-forward, ולכן מי שמגיע שני חייב `rebase` ובדיקה מחדש. זו נקודת האינטגרציה, לא `staging`.

### השער — למה אי אפשר לדלג על הטסטים

`.githooks/pre-push` חוסם דחיפה ל-`main` בשתי בדיקות:

1. `preflight.js` עובר.
2. **בדיוק הקומיט הזה קיים באחד מענפי הבדיקות** — `staging-*` של סשן כלשהו, או `staging` הישן. כלומר הוא באמת נפרס לסביבת בדיקות.

תנאי 2 הוא הלב: הוא הופך "קודם טסטים" ממשהו שצריך לזכור למשהו שאי אפשר לפספס בשוגג. אם לא ראית את הקוד רץ בסביבת בדיקות, הוא לא יעלה לאתר. דחיפה לענפי הבדיקות ולכל ענף אחר עוברת חופשי.

**הפעלה חד-פעמית בכל clone:**
```
git config core.hooksPath .githooks
```

**מעקף לחירום** (תקלה באתר החי שחייבת תיקון מיד): `PG_FORCE=1 git push origin main`. יש מעקף כדי שלא יהיה תמריץ לכבות את השער לגמרי.

**אם `prod` נדחה** — מישהו דחף ל-`main` מאז. `git fetch origin && git rebase origin/main`, ואז `stage` שוב (כדי לבדוק את השילוב) ורק אז `prod`.

**כתובת הבדיקות** נגזרת משם הסשן: `https://phonegat-website-git-staging-<שם>-ofek205s-projects.vercel.app`. `ship.js stage` מדפיס אותה, ו-`ship.js whoami` מראה אותה בלי לדחוף.
כברירת מחדל Vercel מגן עליה ב-Vercel Authentication, כלומר צריך להיות מחובר ל-Vercel באותו דפדפן. אפשר לכבות (Settings → Deployment Protection → Disabled) ואז הקישור ניתן לשיתוף עם כל אחד — מה שמאפשר את זה בבטחה הוא הבאנר הצהוב וה-`noindex`.

### `PG_PROD` — ההגנה שאסור להסיר
`window.PG_ENV` / `window.PG_PROD` מוגדרים בראש ה-`<head>`. **פרודקשן = הדומיין האמיתי בלבד**; `localhost` וכל `*.vercel.app` הם סביבת בדיקות. ארבעה דברים מושתקים שם, כי כל אחד מהם מזהם נתון שאי אפשר להחזיר:

- **GTM/GA4 לא נטען** — בדיקה לא תיספר כלקוח אמיתי. `dataLayer` ו-`track()` ממשיכים לעבוד, כך שחיווט האירועים נשאר בדיק. `?pg_gtm=1` מכריח טעינה כשצריך לבדוק תג.
- **הטופס והליד מהצ'אט לא נשלחים** — פנייה מבדיקה לא תגיע לתיבה של ברוך וסיגל. הוולידציה כן רצה במלואה, והמשתמש מקבל הודעה שזו בדיקה.
- **מונה הקופונים לא נגרע** — `fetchSeq` מחזיר `null` ונופל לטווח `9xxx`, כי ערך המונה **הוא** התשובה ל"כמה קופונים הונפקו".
- **`noindex`** — גם ב-`vercel.json` (כלל `X-Robots-Tag` לכל `*.vercel.app`) וגם כ-meta, כדי שגוגל לא יאנדקס עותק בדיקות במקום האתר.

בנוסף מוצג באנר אדום "סביבת בדיקות" כדי שלא יבלבלו בין staging לאתר החי.

**כשמוסיפים אינטגרציה חדשה שכותבת לעולם האמיתי** (מייל, מונה, תשלום, CRM) — עטוף אותה ב-`if(!window.PG_PROD)` והוסף בדיקה ל-`preflight.js`.

### עריכה מקבילה
לפעמים שני chats עורכים את `index.html` יחד. לפני עריכה: `git pull` ובדיקת `git status`. לפני commit — ודא שאתה מעלה רק את השינויים שלך (אפשר לבודד עם patch חלקי), כדי לא לדחוף עבודה לא-גמורה של הסשן האחר.

**ענף לכל סשן, וענף בדיקות לכל סשן.** כל סשן עובד על ענף משלו (`service-pages`, `spec-existing-pages` וכדומה) ודוחף לבדיקות ב-`staging-<שם>` משלו. הענף הוא מה ששומר על העבודה, ובידוד ענף הבדיקות הוא מה שמונע את ההתנגשות מלכתחילה.

**אסור `push --force` לשום ענף משותף.** עד 31.7.2026 `ship.js stage` הריץ `git push -f origin HEAD:staging`, כלומר החליף את הענף ב-HEAD של מי שהריץ אחרון. באותו יום נמחקו כך ארבע קבוצות של קומיטים, בלי שגיאה ובלי אזהרה, ומי שאיבד אותן גילה זאת רק כי בדק ידנית. היום הדחיפה היא fast-forward בלבד, ומאז 2.8.2026 גם הענף עצמו פרטי לסשן.

**בדיקה לפני שמסתמכים על ענף של מישהו אחר:**
```
git merge-base --is-ancestor <הענף-שלך> origin/<הענף-שלהם>
```

**לפני commit — ודא שאתה מקמט רק את הקבצים שלך.** `git commit -am` סוחף את העבודה הלא-גמורה של הסשן האחר. הדרך הבטוחה היא `git add <הקבצים שלך>` ואז `git commit` בלי `-a`.

---

## דף תוכן חדש — מה חייב לעבור אליו

אין שלב build, ולכן **כל דף נושא עותק משלו** של המסגרת המשותפת. `phone-problems.html` נוצר בלי ארבעה מהם, ואף בדיקה לא תפסה את זה — הדף עלה ל-staging כשהוא מזהם את GA4 ובלי תפריט נגישות. הרשימה הזאת קיימת כדי שזה לא יחזור.

| מה | למה | הבדיקה שתופסת |
|---|---|---|
| **שומר `PG_ENV`/`PG_PROD`** בראש ה-`<head>`, **לפני** GTM | בלעדיו בדיקה נספרת ב-GA4 כלקוח אמיתי | בדיקה 4 + 8 |
| **גידור GTM** ב-`if(window.PG_PROD\|\|…pg_gtm=1)` | אותו דבר | בדיקה 8 |
| **תפריט נגישות** (`#a11yTrigger` + `#a11yPanel` + ה-JS + כל 10 כללי ההשפעה) | דרישה רגולטורית. דף בלי כפתור מחייב לעבור לדף אחר כדי להגדיל טקסט | בדיקה 10 |
| **באנר קוקיז** (`#cookieOk`/`#cookieNo`) | דף נחיתה בלי באנר = מי שבא מגוגל נשאר ב-`denied` ואין לו איך לאשר, כלומר **התנועה לא נמדדת** | בדיקה 10 |
| **רישום service worker** | דף נחיתה הוא לעיתים הדף היחיד שנפתח | בדיקה 10 |
| **`canonical` לדומיין האמיתי** | | בדיקה 8 |
| **ישות `#business` מוגדרת**, לא רק מוזכרת ב-`@id` | הפניה בלבד לא נפתרת בהכרח בגוגל | בדיקה 9 |
| **`track()` ל-CTA** | בלי זה אין דרך לדעת אם הדף ממיר | — |

**כשמוסיפים כלל השפעה חדש לתפריט הנגישות** (`html.a11y-*`) — הוסף אותו **בכל** דפי התוכן. ווידג'ט שמציג בקרה שלא עושה כלום גרוע מהיעדר בקרה.

### עמוד שמדבר על ההווה חייב חותמת טריות

עמוד שאומר לקורא "כך זה נראה היום" אינו נשבר כשהוא מתיישן, הוא רק הופך למטעה, ואין לזה שום סימן חיצוני. לכן עמוד כזה נושא:

```html
<time id="pg-checked" datetime="2026-08-09">9 באוגוסט 2026</time>
```

**בדיקה 27 חוסמת העלאה כשהתאריך ישן מ-90 יום**, מזהירה מ-75, ונכשלת גם על תאריך עתידי כי הוא תמיד טעות הקלדה שמשתיקה את השער לחודשים. הבדיקה גנרית: כל עמוד שיישא את התגית יקבל את אותו שער בלי לגעת בקוד. היום זה `/upcoming-phones/` בלבד.

**כשמעדכנים עמוד כזה — לעדכן גם את `datetime` וגם את הטקסט.** שניהם באותה תגית, כדי שלא ייפרדו.

### מכשיר ייחוס: דגם שאיננו מוכרים

`status: "reference"` הוא דגם שפון גת **אינה מוכרת**, והוא במאגר אך ורק כדי שאפשר יהיה להשוות אליו. הוא **אינו מקבל עמוד מכשיר**, אינו ב-`/phones/`, אינו מוצע בשאלון ואינו נדרש ב-`DEVICE_ORDER`. הוא כן מופיע בעמודי השוואה, ושם הוא מסומן `.noown` במקום קישור, כי אין לאן לקשר.

**למה זה קיים:** אופק ביקש להוסיף מותגים כמו OnePlus כדי שאפשר יהיה להשוות. עמוד מכשיר שנראה כמו עמוד A57 אומר ללקוח שאנחנו מוכרים אותו, וזו בדיוק הבעיה שנוצרה בטעות ב-`galaxy-a56` וב-`xiaomi-15`. כאן היא הייתה נוצרת בכוונה.

**עמוד השוואה שכולל מכשיר ייחוס פותח בגילוי נאות** שאיננו מוכרים אותו. זה לא נימוס: מי שקורא השוואה צריך לדעת שלכותב יש אינטרס בצד אחד.

**שלושה מותגים נבדקו ב-10.8.2026:** ל-OnePlus יש יבואן רשמי (סל נאו) ול-Nothing יש יבוא רשמי דרך קמעונאים. **ל-Google Pixel אין יבואן רשמי בישראל כלל**, והוא מגיע ביבוא מקביל בלבד, ולכן עמוד עליו יחייב את אותו גילוי כמו A07.

### הניווט נכתב על ידי מחולל, לא ביד

`node .claude/tools/gen-nav.js` כותב את הניווט הראשי, ה-CSS וה-JS שלו, ל-57 העמודים. **אל תערוך `<nav class="main">` בקובץ בודד** — בדיקת הפריפלייט משווה את תוויות הניווט בכל עמוד מול `index.html`, ולכן עריכה במקום אחד שוברת את כל השאר.

- **עמוד חדש שאמור להופיע בתפריט** נוסף ל-`structure()` שבראש המחולל.
- **דגם חדש ב-`devices.json`** חייב להופיע ב-`DEVICE_ORDER`, אחרת המחולל **נופל בכוונה**. זה מונע דגם שנוסף למאגר ונעלם מהניווט בשקט.
- המחולל **בר-הרצה חוזרת**: ה-CSS וה-JS עטופים בסימני `gen-nav:css:start` ו-`gen-nav:js:start`, ולכן הרצה שנייה מחליפה במקום להוסיף עותק.
- הוא **מסרב לרוץ** אם יעד בתפריט אינו קיים על הדיסק. קישור מת בניווט הוא קישור מת ב-57 עמודים בבת אחת.
- `accessibility.html` ו-`privacy.html` **כלולים מאז 17.8.2026.** עד אז הם היו העמודים היחידים בלי הדר, בלי פוטר ובלי ניווט, ומי שנחת בהם קיבל רק קישור "חזרה לאתר". שני שיעורים מהעברה: קישור הדילוג היה ב-`z-index:20` והיה נפתח מתחת להדר ה-sticky שב-100, ולכללים הכלליים של העמוד (`a{color:var(--teal-d)}`, `li{margin}`, `h4` בלי `margin:0`) יש השפעה על המסגרת, וקישורי הפוטר יצאו טורקיז על שחור ביחס 2.2:1. שני אלה נמצאו בהשוואת סגנון מחושב מול `/guides/` ב-iframe, ולא בעין.

**במסך עם עכבר התפריט נפתח גם במעבר עכבר, ובמגע בקליק בלבד.** hover **בנוסף** לקליק זה בסדר, hover **במקום** קליק הוא הכשל הנגישותי הנפוץ ביותר בתפריטים כאלה, ולכן הקליק עובד תמיד. השער הוא `(hover:hover) and (pointer:fine)`, ובלעדיו מכשיר מגע יורה `mouseenter` סינתטי בהקשה והתפריט נפתח ונסגר מעצמו.

יש **גשר שקוף** (`.npanel::before`) שמכסה את הרווח בין הכפתור לפאנל. בלעדיו העכבר בדרך אליו עובר מעל כלום, `mouseleave` יורה, והתפריט נסגר באמצע התנועה. Escape סוגר ומחזיר פוקוס לכפתור, וחיצים מנווטים בתוך הרשימה.

**בדיקת כיסוי:** אחרי שינוי בניווט שווה להשוות את יעדיו מול העמודים שעל הדיסק. כך התגלה ש-`/phones/compare/` נשכח. מה שנשאר מחוץ לניווט בכוונה הוא שני העמודים המשפטיים, שמקושרים מהפוטר, ועשרת עמודי ההשוואה הבודדים, שמרוכזים ב-`/compare/`.

**המסגרת המשותפת חייבת להיראות זהה בכל הדפים.** כפתורים, פוטר וניווט — אותן מחלקות ואותם ערכים כמו ב-`index.html`. הכותרות בפוטר ובהדר נשארות ב-`--font`; הסריף העריכותי שייך ל-`main` בלבד.

### טבלה שמצהירה על הקטלוג נגזרת, לא נכתבת ביד

`node .claude/tools/gen-guide-tables.js` כותב את טבלת דירוגי ה-IP ב-`/guides/first-phone-for-kid/` ואת טבלת נפחי האחסון ב-`/guides/how-much-storage/`, משתיהן מ-`devices.json`. **אל תערוך את מה שבין סימני `gen-guide-tables:*:start` ו-`:end`** — ההרצה הבאה תדרוך על זה.

הוא נוצר ב-19.8.2026 אחרי שהתגלה שהשתיים התיישנו בשקט: הראשונה הצהירה "דירוג העמידות של **כל דגם** שאנחנו מוכרים" והחסירה עשרה מ-21, והשנייה החזיקה 17 דגמים עם הערה שאומרת "ב-5 דגמים בלבד מתוך 17". הצהרת השלמות היא בדיוק מה שנשבר כשהקטלוג גדל, ואין לה שום סימן חיצוני.

**המספרים חיים בטבלה, לא בפרוזה שסביבה.** משפט כמו "יש ארבע רמות" הוא עותק שני של נתון, והוא נפרד ממנו בשקט. הפרוזה בשני העמודים נוסחה כך שאינה נוקבת במספרים, וההערה היחידה שכן נוקבת עטופה ב-`gen-guide-tables:slotcount`.

**וזה גם כלל כתיבה, לא רק מנגנון:** אסור לכתוב בקופי טענה שנכונה רק ביחס לקטלוג — "היחיד עם", "הכבד ביותר", "שני הדגמים היחידים ש". טענה כזאת נכונה ליום שנכתבה בו, נשברת בכל דגם שנוסיף, ואי אפשר לאכוף אותה בבדיקה כי רגקס אינו יודע לספור תכונות. באותו יום נמצאו שש טענות כאלה בעמוד Redmi Note 15 Pro, וחמש מהן היו לא נכונות גם מבין 21 הדגמים שאנחנו מוכרים. במקום זה נסח את העובדה ישר: "יש בו משדר אינפרא-אדום" נכון תמיד. הכלל מלא ב-`devices.json` ב-`_rules`, ברשומה שמתחילה ב-"copy: אסור לכתוב טענה שנכונה רק ביחס לקטלוג". `_rules` הוא מערך: כלל שנוסף כמפתח עם שם (`_rules.x = ...`) נשמט בשקט בכתיבה, וכך בדיוק הכלל הזה לא נשמר עד 24.9.2026.

**⚠ `\b` אינו עובד ליד עברית.** גבול מילה ב-JS מוגדר על `[A-Za-z0-9_]`, ואותיות עברית מחוץ לקבוצה, ולכן `/אין\b/` אינו נתפס אף פעם. זה הפיל אצלי כל שורה בטבלה, וגם השתיק שבע תבניות ב-`copy-audit.js` שדיווח "נקי" מבלי לבדוק. כשצריך גבול, כתוב אותו במפורש: `(?:^|[^\u0590-\u05FF])`.

### דגמים שעוד לא יצאו: שמועות עם מקור, ובלי שום הבטחה

`node .claude/tools/gen-upcoming.js` כותב את `/upcoming-phones/<slug>/` מתוך `.claude/data/upcoming.json`, ואת המקטע "הדגמים שבדרך" בעמוד הרכזת `/upcoming-phones/`. הוא הבעלים של כל ה-`<main>` בעמודים האלה, של ה-JSON-LD שלהם ושל ה-title והתיאורים. **אל תערוך אותם ביד.** נוצר ב-24.9.2026 לבקשת אופק, כדי להופיע בחיפושים על דגמים לפני שיוצאים.

- **כל שמועה עם מקור.** בלי מי, קישור ותאריך, המחולל עוצר ולא כותב אף עמוד. מידע שמסומן "רשמי" חייב מקור ב-apple.com. כך נשמרת ההבטחה בעמוד הרכזת שלא נכתוב "תאריכים שאין להם מקור".
- **לא מבטיחים כלום, כדי שלא תהיה חשיפה לתביעה**, הוחלט עם אופק באותו יום. אין מחיר, גם לא המחיר האמריקאי. אין מועד בשמנו, ותאריך שאפל פרסמה נכתב "לפי אפל" ועל המדינות שהיא מנתה. אין "נעדכן אתכם", כי גם זו התחייבות. המחולל סורק את הטקסט ועוצר על מילות הבטחה ועל מחיר, והצהרת אי-ההתחייבות נכתבת על ידו בראש כל עמוד ובסופו.
- **עמוד כזה מתיישן הכי מהר באתר**, ולכן הוא נושא `pg-checked`, ובדיקה 27 חוסמת אותו אחרי 90 יום. כשדגם מוכרז רשמית, הוא עובר ל-`devices.json` ולעמוד מכשיר, והעמוד כאן מתעדכן או יורד.

**ובאותו יום, בעמודי המכשיר:** דגם שהוכרז ועוד לא בחנות מקבל `commercial.not_in_store: true`. בלי הדגל התבניות כתבו "שני המכשירים אצלנו בחנות" ו"אפשר לבוא להחזיק את המכשיר" גם על אייפון 18 פרו, שהמחיר שלו בישראל עוד לא נקבע. כשהדגם מגיע לחנות, מוחקים את הדגל.

### שעונים ואוזניות: קטגוריות בכלי ההשוואה

`gen-compare.js --watches` ו-`gen-compare.js --headphones` בונים כלי השוואה (`/watches/compare/`, `/headphones/compare/`) ועמודי השוואה קבועים ב-`/compare/<slug>/`, מתוך `watches.json` ו-`headphones.json`. **הקבצים נפרדים מ-`devices.json` בכוונה:** כל צרכן של `devices.json` מניח טלפון, ושעון שהיה נכנס לשם היה מופיע בשאלון ובטבלאות המדריכים. כל מה שנבדל בין הקטגוריות הוא טקסט, והוא כתוב פעם אחת בטבלה `CATS` בראש המחולל. קטגוריה חדשה היא רשומה שם וקובץ נתונים, וכלי הטלפונים, מרכז ההשוואות והמדידה (`pg-compare-track`, שצריך להכיר את הנתיב) מקשרים אליה.

- **איננו יודעים אילו שעונים ואוזניות בחנות.** לכן אין בעמודים האלה "אצלנו בחנות", רק שאלה. עד 24.9.2026 כלי השעונים ירש מכלי הטלפונים את "המכשירים אצלנו בחנות" ואת הקישור "המפרט המלא" ל-`/phones/<slug>/`, כלומר ל-404.
- **הקווים היחסיים בטבלה רק לשדות ב-`_numok`**, אותו כלל שהכלי מקבל. באוזניות הוא ריק: קו בין אוזנייה של 5 גרם לאוזניות קשת של 386 גרם משווה שני דברים שונים.
- **הנתונים של אוזניות נאספו מטקסט גולמי של עמודי היצרן**, וכל ערך נבדק שהציטוט שלו נמצא מילה במילה במקור ושכל מספר נמצא בציטוט. לאפל אין עמודי מפרט ישראליים ל-AirPods, ולכן הגלובליים. סתירה בין שני מקורות של אותו יצרן מוצגת כסתירה ולא מוכרעת בשקט.
- **שורת המקורות מתחת לכל טבלה**, בכל עמודי ההשוואה, נגזרת מ-`spec_source`. שדה שנלקח ממאגר מפרטים ולא מהיצרן מופיע שם כך.

### כלי ההשוואה: הקוד בקבצים משלו (מ-24.9.2026, עיצוב ח׳ מהקנבס)

שלושת הכלים (`/phones/compare/`, `/watches/compare/`, `/headphones/compare/`) נבנים מ-`.claude/tools/compare-tool/`: `tool.client.js` הוא הקוד שרץ בדפדפן, `tool.css` העיצוב, ו-`means-phones.json` ההסברים לשדות של טלפון. **ערכו שם, לא בתוך `gen-compare.js`.** עד אז הכלי היה מחרוזת של 58KB בתוך המחולל, ושעונים ואוזניות קיבלו אותו דרך שמונה החלפות טקסט. היום ההבדלים בין הקטגוריות עוברים כנתונים ב-`CFG`.

- **שלושה שמות מחלקה נשמרו בכוונה:** `button.dopen` עם `data-slot`, `button.dchip` עם `data-slug` ו-`aria-pressed`, ו-`nav.catsw`. בלוק המדידה, שזהה ב-100 עמודים, מזהה לפיהם צד, בחירת דגם ומעבר בין כלים. שינוי שם משתיק את `compare_select_model` בשקט.
- **אל תזכירו את שם בלוק המדידה בהערות של הכלי.** בדיקת אחידות המדידה ב-preflight מאתרת אותו לפי המופע הראשון של השם בעמוד, והערה ב-CSS הפנתה אותה לסקריפט אחר.
- **`add-deals.js` מסרב ל-`open` בשם.** עד הכלי החדש הוא סורב רק במקרה, כי הכלי הישן נשא `class="mean open"`. בלי זה כללי `.open` מדף הבית נכנסו ל-88 עמודים.
- **ההסבר לכל שדה בכלי נפתח מכפתור i, מתחת לשורה.** הסבר קבוע מתחת לכל שדה נוסה ב-24.9.2026 והוסר באותו יום, כי אופק אמר שהוא מציף את העין.
- **עמודי ההשוואה הקבועים באותה שפה** (`buildMain` ב-`gen-compare.js`, ו-`page.client.js` ל"מה חשוב לכם"). כל התוכן נבנה ב-HTML בשביל גוגל, ו-JS רק מסדר. אין בהם משפט הסבר מתחת לשדה: הוא זהה בכל העמודים והיה מקרב אותם לתבנית אחת.
- **אזור נתונים שאינו טבלה מסומן `data-pg-data`**, ו-`copy-audit.js` מדלג עליו כמו על `<table>`. בלי הסימון הבדיקה "נתון שחוזר שלוש פעמים" סופרת את המפרט עצמו כגוף.
- **שניים כברירת מחדל, ושלושה כשמבקשים.** הכפתור "הוספת דגם שלישי" מופיע אחרי שיש זוג, והצד השלישי כחול (Okabe-Ito, נבדל מהסגול ומהכתום גם בעיוורון צבעים). "משווים גם" מחליף צד ולא מוסיף, ומוסתר כשיש שלושה. עמודי ההשוואה הקבועים נשארים זוגות.

---

## קודי קופון — חובה לכל מבצע חדש

לכל מבצע עם קוד קופון **חייב** להיות קוד בפורמט `XX-NNNN`: שתי אותיות שמזהות את **סוג** המבצע (לא את המחיר), ואחריהן מספר סידורי רצוף מהמונה בשרת. המספר האחרון שהונפק הוא התשובה לשאלה "כמה קופונים נוצרו".

**זו התנהגות ברירת המחדל — אין צורך לבקש אותה.** כל מבצע חדש שנוסיף יעבוד כך.

### שלושה מקומות לעדכן, תמיד ביחד

1. **`prototype/index.html`** — הוספת רשומה ל-`OFFERS` בבקר הקופונים:
   ```js
   tradein:{title:'…',desc:'…',price:'…', … ,key:'pg_tradein_coupon',prefix:'TR-', … }
   ```
   `prefix` — שתי אותיות שעוד לא בשימוש. `key` — מפתח `localStorage` ייחודי.

2. **`prototype/api/coupon.js`** — הוספת מזהה המבצע לרשימת ההיתר:
   ```js
   var OFFERS = { screen: 1, kb: 1, tradein: 1 };
   ```

3. **הטריגר ב-HTML** — `<button data-pg-coupon="tradein">`. ערך ריק (`data-pg-coupon`) = מבצע מגן המסך.

### ⚠️ המלכודת
אם תוסיף מבצע ל-`OFFERS` בלקוח **ותשכח את רשימת ההיתר בשרת**, השרת יחזיר 400, והלקוח ייפול בשקט לקוד בטווח `9xxx`. האתר ימשיך לעבוד, הלקוחות יקבלו קודים תקינים — **אבל המבצע לא ייספר בכלל**. אין שגיאה גלויה למשתמש. תמיד עדכן את שני הצדדים.

### עקרונות שאסור לשבור
- **`POST` בלבד להנפקה.** תשובת `GET` עלולה להיות מוקצדת ב-CDN, ואז שני לקוחות יקבלו את אותו מספר.
- **הפופ-אפ לא מחכה לרשת.** נפתח מיד עם פלייסהולדר `••••`, וכפתורי ההעתקה וה-WhatsApp חסומים עד שהקוד מגיע — כדי שלא יישלח פלייסהולדר ללקוח.
- **קוד ששמור ב-`localStorage` מוצג סינכרונית** ולא צורך מספר חדש. לקוח חוזר רואה את אותו קוד.
- **הטוקן של המונה נשאר בצד שרת בלבד.** אסור להזכיר אותו ב-HTML/JS של הדף.
- **טווח `9xxx` שמור לנפילות.** אל תנפיק ממנו מספרים רגילים.

### קריאת המדד
`pg:cpn:seq:<offer>` ב-Upstash. הערך של המפתח = כמה קופונים הונפקו למבצע הזה.

שתי דרכים לקרוא אותו, ואף אחת מהן אינה צורכת מספר:

**מהדשבורד:** Vercel ← `phonegat-website` ← Storage ← מסד ה-KV ← Data Browser, ושם `pg:cpn:seq:screen`, `pg:cpn:seq:kb`, `pg:cpn:seq:idf`.

**מהשורה:**
```
curl -s https://www.phonegat.co.il/api/coupon/ -H "x-pg-stats: $PG_STATS_TOKEN"
```
מחזיר `{"issued":{...},"total":N,"at":"…"}`. **הלוכסן בסוף חובה:** `vercel.json` מגדיר `trailingSlash: true` מאז 17.8.2026, ובלעדיו התשובה היא 308 ריק. גם הלקוח קורא ל-`/api/coupon/` עם הלוכסן מאז 25.9.2026. עד אז כל קופון עבר הפניה: הדפדפן עקב עם אותו POST והקופון הונפק, אבל כל הנפקה עלתה סיבוב רשת נוסף. **כתובת חדשה לנקודת הקצה נכתבת עם לוכסן בסוף.** ה-`GET` קורא בפקודת `GET` ובטוקן הקריאה-בלבד, ולכן אינו יכול להגדיל שום דבר. **בלי `PG_STATS_TOKEN` מוגדר בסביבה הוא לא קיים**, וגם עם טוקן שגוי התשובה היא אותו 405 של כל מתודה אחרת, כדי שלא יימצא בגישוש.

**אי אפשר להוציא את הטוקנים דרך ה-CLI.** `vercel env pull` מחזיר את שמות המשתנים עם ערכים ריקים, כי הם מגיעים מאינטגרציית Upstash ומסומנים רגישים. זה נבדק ב-16.8.2026, אז אל תבזבז על זה סיבוב.

---

## כתיבה ותוכן
- **אל תשנה טקסט של ביקורות לקוחות.** הן אמיתיות. גם כשמתקנים עובדות באתר — לא נוגעים בציטוטים.
- "WhatsApp" באנגלית, לא "וואטסאפ".
- מספרי טלפון בתצוגה: `052-5893366` / `08-6812050`. בקישורי `tel:`/`wa.me` — ספרות בלבד.
- לא להשתמש ב-"חינם" (מזלזל במותג) — במקום זה "ללא עלות".
- **אין מקף ארוך (—) בשום טקסט שהקורא או גוגל רואים** — גוף, כותרות, `alt`, `title`, `meta`, JSON-LD. בעברית אף אחד לא מקליד אותו ביד, ולכן הוא הסימן הבולט ביותר לטקסט שנכתב במכונה. פסיק, נקודה או נקודתיים במקומו. מקף בינוני בטווחים (`א׳–ה׳`, `9:00–18:30`) תקין ומותר, והערות קוד לא נבדקות. אוכף: בדיקה 13 ב-`preflight.js`. שאר סימני הכתיבה במכונה, שרגקס לא תופס, מפורטים ב-`pg-new-content-page` §7.
